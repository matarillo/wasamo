---
phase: M4-Phase 2
task: T5
title: ウィンドウごとのフォーカス状態と Tab 走査
date: 2026-08-07
scope: task-end
merge_target: feat/m4-phase-2
---

# T5 レトロスペクティブ

タスクブランチ: `feat/m4-phase-2-t5`（`feat/m4-phase-2` = `6ef83b4` から作成）

スパイクの走査中核に最初の production 呼び出し元を与え、`WindowState` に
フォーカスの記録を 1 つ置き、Tab / Shift+Tab を host のキー枠より先に
消費し、消費しなかったキーを `DefWindowProc` へ落とすようにした。
フォーカス表示は hover と同じブラシの第 2 の軸として実装した。

plan は本 task を「中核に production 呼び出し元を与える」と framing して
いたが、実際に重心が乗ったのは 2 か所である。1 つは **証拠**（キー経路に
触れるテストが repo に 1 本も無く、hover で T4 が踏んだのと同型だった）。
もう 1 つは、**レビューで見つかった到達可能なクラッシュ**である。

## コミットの概要

| commit | 内容 |
|---|---|
| `11585b7` | start gate（実測 6 件、規範文の対応表 10 行、罠 1〜5・7 該当／6 armed、review lane 確認、境界 3 点の明示、§13.3 の divergence 記録） |
| `0947998` | production（`focus.rs`、`ButtonData.focused` と `set_button_focused_at`、`effective_button_color` の焦点軸、`WM_KEYDOWN` 経路、クリック時のフォーカス、test seam 2 件、単体 9 本） |
| `f84b450` | `tests/focus_traversal_integration.rs`（fixture 5 本） |
| `05d46f9` | stale-id クラッシュの修正＋回帰 fixture（F6）＋ GUI 捕捉スクリプト |
| `24949f0` | close gate（監査表、構造的副作用、幾何書き込み列挙、並行データ、分岐表、変異証人 9、GUI 陽性対照、前送り 6、task list 再監査）＋ plan の T5 改訂 |

## チェックリスト（task-end、項目 1〜11）

1. **今回の主な学び。**

   **(a) 変異証人は「自分の実装の変異」しか測れない。設計が持つ未表現の
   前提は測れない。**
   本 task で見つかった唯一の重大欠陥（保持した `FocusId` が投影より
   長生きしてパニックする）は、fixture 6 本・単体 9 本・変異証人 8 本の
   **どれ 1 つも赤にしない**。見つけたのは `move_focus` を読んでいて
   「木が縮んだらこの id は何を指すのか」と問うたときである。T4 の是正
   （復元変異を必ず 1 本入れる）は効いた（W1 が 4 fixture を赤にした）が、
   **効いた上でこの欠陥は通り抜けた**。復元変異が答えるのは「この機能が
   無い状態を捕まえるか」であって、「この機能の前提が破れた状態を
   捕まえるか」ではない。

   **(b) 否定的予測を 1 回測る是正（T4 導入）が、assertion の形そのものを
   変えた。**
   「消費しなかったキーの `DefWindowProc` 落としは戻り値では区別できない」
   を 8 key 実測して確定させた結果、`LRESULT(0)` を assert して満足する
   のではなく、**host のキー枠にレコーダを差す**という別の判別手段に到達
   した。測らなければ「Tab は 0 を返す」で終わっていた —— これは消費でも
   落としでも真である。W7（枠を走査より前に呼ぶ変異）は、この書き換えが
   無ければ何も赤にしなかった。

   **(c) 型で表現できる不変条件は型で表現する。**
   修正の中心は `FocusProjection::path` を `Option` 返しにしたことで、
   `discard_stale_focus` はその補助である。裸の index は「呼び手が確認
   する」規律を要求し、規律は破れる。`Option` は呼び出し箇所で忘れられ
   ない。

   **(d) 純ロジックの core を「採用する」と決めたとき、その core の
   識別子契約も一緒に採用している。** `FocusId` = 投影の pre-order index
   というのはスパイクの設計であり、`focus_core` のドキュメントにはある。
   しかし DD-M4-P2-003 は「スパイクの走査中核を実装として採用する」とだけ
   書き、**その id がメッセージをまたいで保持できるかどうか**には触れて
   いない。production 化する task がそこを問わなければ誰も問わない。

2. **仕様文書の変更。** なし。
   [dsl_spec.md §4.19](../../../../docs/dsl_spec.md) の Focus 節は landed と
   一致（開いた時点で無フォーカス、宣言順で両端 wrap、無効 Button は
   スキップ、クリックは直近の focusable へ・背景では消えない、
   ウィンドウの活性化では動かない）。各項に対応する fixture 名を close
   gate の分岐表に書いたので、T13 は一覧の照合で足りる。
   [architecture.md §13.3](../../../../docs/architecture.md) の指標の
   1 文だけは landed と食い違う（項目 4）。本 task では直さず、T13 の
   再検証項目として記録した。

3. **post-commit 検証。** green。
   post-commit state に対して `cargo fmt --all -- --check` ゼロ終了、
   `git diff --check` clean。`cargo clean`（10,306 files / 2.8 GiB 除去）の
   後、`cargo build --release --workspace` 1m10s 成功、
   `cargo build --workspace` 1m03s 成功、
   `cargo test --workspace --no-fail-fast` は
   **44 binary/section すべて `ok`、1,051 passed / 0 failed / skip 0**
   （T4 の 1,036 に対し、`focus.rs` 単体 7 + `widget.rs` 単体 2 + 統合 6）。
   skip 0 は Compositor が使える環境で走ったことを意味する。GUI 陽性対照は
   release ビルドに対して別途実行済み（close gate §#7）。

4. **PO に相談すべき設計判断・トレードオフ。** **あり 2 件、同日決着**
   （どちらも landed コードは変えない。決まったのは着地先）。

   **(A) `architecture.md` §13.3 のフォーカス表示の 1 文が literal には
   実装不能である。**「視覚幾何を書くのと同じパスで適用される」と書いて
   あるが、そのパス（`sync_visuals`）はレイアウトからしか走らない。Tab は
   状態書き込みではないのでレイアウトを起こさず、そこで適用すると
   **別の何かが再レイアウトするまで表示が出ない**。同じ文の後半
   「フォーカス変更時に Visual を書くのではない」は landed が literal に
   満たしている（新規 `Visual` ゼロ、`SetOffset` / `SetSize` は 6 個の
   ままで全部 `sync_visuals` の中）。DD-M4-P2-003 の技術リスク節自身が
   緩和策を「列挙という artifact」と定めており、禁止とは書いていない。
   **決着（2026-08-07）: T13 の再検証で文言を landed に合わせる**（前半を
   「hover / pressed と同じ手段で適用される presentation state」に）。
   [plan.md](../implementation/plan.md) の T13 の確認項目に 1 行として
   起こしてあるので、義務は実行される場所で見える。
   **あわせて、枠線／囲み線でフォーカスリングを描けるようにすることを
   [候補プール](../../../candidate-pool.md)に 1 行追加した（マイルストーン
   指定なし）。** 文言を直すのは食い違いの解消にすぎず、**背景色しか手段が
   ないという制約そのものは残る** —— リングが描けるなら、hover・選択・
   フォーカスの 3 つが 1 つの手段を奪い合う状況自体が消える。制約の解消を
   文言修正で閉じてしまわないための行である。

   **(B) 合成キー入力は、送る前にフォアグラウンドを取得して確認する必要が
   ある。** マウス入力はカーソル位置で宛先が決まるので活性化を要さないが、
   キー入力は**フォアグラウンドスレッドのフォーカス窓**へ届く。したがって
   活性化を持たずにキーを合成すると別の窓へ行く（セッションにフォア
   グラウンド窓が無ければどこへも行かない）。`SetForegroundWindow` は
   呼び手が既にフォアグラウンドでないと拒否されるため、活性化は**実クリック
   で獲得して読み戻す**しかない。

   **決着（2026-08-07）: これは機械の性質ではなく手順の規律である。**
   捕捉機構の規律が住む
   [verification-environments.md](../../../../docs/notes/verification-environments.md)
   の Observation 4 に、PMv2 の読み戻し規則の隣に 2 項として畳んだ ——
   (1) キー入力を合成するものは活性化を獲得し・確認し・**再試行**する、
   (2) 代わりに `WM_KEYDOWN` を投げる捕捉は**弱い主張**であり、どちらの
   経路を使ったかを記録する。あわせて
   [plan.md](../implementation/plan.md) の T12 に、control B が Tab 駆動
   である以上この手順を踏むこと・使った経路を記録することを明記した。

   なお報告のみ 1 件: フォーカス色は既存色に固定色を半分ずつ混ぜる暫定で
   ある。M5 のテーマ面が吸収しうる（[dsl_spec.md §4.18](../../../../docs/dsl_spec.md)
   が selected 系の見た目を安定性の約束から外しているのと同じ扱い）。

5. **タスク外のリファクタ・構造変更。** なし。
   `spike_focus_role` → `focus_role` のリネームは production 化と不可分
   （呼び出し元は `focus_spike` 1 か所）。`focus_core.rs` / `lib.rs` の
   module doc 訂正も同様で、「production 呼び出し元は無い」が本 task で
   偽になるため放置できない。

6. **追加 DD の要否。** 不要。
   判断はすべて DD-M4-P2-003 の範囲内である。lazy な successor は
   「除去前に後継を計算する」を **materialisation seam を持つ task
   (T7)** に委ねた結果であって、規則の変更ではない（CF-T5-2）。
   §13.3 の文言差は spec 側の再検証項目であって新しい決定ではない。

7. **新規 Proposed ADR 項目 / 昇格。** なし。

8. **マイルストーン AC / フェーズ構成の追加・変更。** なし。

9. **後続へ持ち越す仮実装・近似・新規 `dead_code`。** あり 2 件。
   - `discard_stale_focus` は、T7 が entry / exit で正しい後継を計算する
     ようになれば存在理由が消える暫定である。T7 の再監査項目に入れた。
   - F3 が無効 Button を `ToggleButton` のリテラル／`Button` の state 束縛で
     書いている。T3 の CF-2（平の `Button` のリテラル `enabled: false` が
     loader で黙って捨てられる）が T8 で直るまで継続。fixture の doc に
     理由と CF-2 参照を明記済み。
   `dead_code` 警告はゼロ。`focus_core.rs` の `#![allow(dead_code)]` は
   残しているが、理由（Group / ActiveItem / ModalScope は T7 まで
   production から到達しない）を書き直した。

10. **新規の cross-task / cross-phase 設計制約。** あり 6 件。
    全文（証拠と再発の合図つき）は
    [log.md](../implementation/log.md) §T5 close gate の前送り表。

    - **CF-T5-1: 範囲内だが別ノードを指す `FocusId` は検出されない** —
      分類 `carry-forward`。範囲外は修正済み、範囲内は T4 の CF-T4-1 と
      同じ index ずれの類。再発の合図は T7 / T9。
    - **CF-T5-2: 後継の計算が lazy である** — 分類 `carry-forward`。
      再発の合図は T7（materialisation seam）。
    - **CF-T5-3: fixture はフォーカス表示の見た目を見られない** —
      分類 `carry-forward`。W6 相当の変異（W8）が何も赤にしないことで
      測定済み。再発の合図は T12。
    - **CF-T5-4: キー入力を合成するものは活性化を獲得・確認・再試行する**
      — 分類 `doc-folded`（[verification-environments.md](../../../../docs/notes/verification-environments.md)
      Observation 4 に畳んだ）。再発の合図は T12 / T11。
    - **CF-T5-5: host のキー枠が証拠として荷重を持った** — 分類
      `carry-forward`。再発の合図は T8（枠の手前に dispatch を入れると
      F5 が落ちる。それが意図した仕掛け線）。
    - **CF-T5-6: §13.3 の指標文言が literal に実装不能** — 分類
      `finding`（持ち主 = T13 の再検証）。項目 4 (A)。

11. **後続タスクリストの見直し。** 必要 → 実施済み。
    T5 の checkbox を `[x]` に。T5 節を 7 点改訂（クリック時のフォーカスを
    dispatch より前に置く理由、キーの walk を T8 へ送る判断、`LRESULT` で
    は判別できないという実測とその代替、幾何書き込みゼロと §13.3 の
    divergence、ギャラリーの第 1 stop が checked である事実、lazy な
    successor、stale-id クラッシュ）。T6〜T13 への影響は log の再監査に記録
    （T7 = CF-T5-1 / CF-T5-2 と `focus_spike` 撤去、T8 = CF-T5-5、
    T11 = クリック時フォーカスを継承しない、T12 = CF-T5-3 / CF-T5-4、
    T13 = CF-T5-6）。

## 目標・前提・計画仮説の再点検

> 後続タスクの retrospective でも流用するダブルループ振り返り欄。
> 実行の巧拙（シングルループ）でなく、**設定した目標・前提・事前計画
> （= 検証すべき仮説）自体が妥当だったか**を、反証可能な signal とともに
> オーナーと問い直す。下の 5 サブセクションがひな型。

### 観測された事実（反証可能な signal）

- **重大欠陥 1 件が、テストではなくコード読解から出た。** 変異証人 8 本
  （W1〜W8）はどれも stale-id クラッシュを赤にしない。W9 は欠陥を知った
  **後**に作った証人である。
- **W1（修正前の挙動の復元）は 4 fixture を赤にし、残り 43 binary は緑の
  ままだった。** キー経路に regression の対象が存在しなかったことの測定。
  T4 の hover と同じ形が 2 task 連続で出た。
- **subagent が書いた fixture の判別脚が、2 task 連続で 1 本ずつ非判別
  だった。** T4 は F5 の座標 1 行。今回は F5 の `LRESULT` 脚 ——
  「Tab が `LRESULT(0)` を返す」は消費でも落としでも真なので、どちらの
  実装でも通る。今回は lead の読解で捕まえた（T4 は変異でしか捕まらな
  かった）。
- **否定的予測を実測する是正が、証拠の形を変えた。** 8 key の測定が
  「戻り値では区別できない」を確定させ、host のキー枠という別の判別手段に
  行き着いた。
- **plan の T5 節は、`FocusId` の寿命に一言も触れていなかった。** 「`FocusState`
  を `WindowState` に置く」「投影が Stop / Container を導出する」とは書いて
  あるが、その id が次のメッセージまで同じものを指すかは書かれていない。
- **「環境にその能力が無い」という結論を、再試行を 1 回もしていない
  サンプルから出した。** 捕捉スクリプトでフォアグラウンド獲得に失敗した
  とき、私はそれを機械の性質（間欠的な能力）と読んだ。実際には、
  獲得手順を尽くしていなかった —— 最初は活性化を「要求」しただけで
  獲得しておらず、次はクリックで獲得したが 1 回で諦めていた。**取りに
  行く手順を尽くす前に、能力の有無を結論している。** オーナー指摘。

### 自己分析（どの層が外れたか）

外れたのは **「テストと変異証人で実装の正しさは担保できる」という
前提**の層である。

T4 の是正で入れた「復元変異を 1 本」は、`W1` として正しく機能した ——
fixture 群が**機能の不在**を捕まえることを示した。だがクラッシュは
機能の不在ではなく、**設計が暗黙に置いた前提（保持する識別子は次に読む
ときも同じものを指す）が破れた状態**である。実装の変異はこの層に届かない。
変異は「書いた式を壊す」道具であり、「書かなかった前提」を壊せない。

もう 1 層は計画側。`FocusId` = 投影の pre-order index という契約は
`focus_core.rs` に書いてあり、スパイク自身が「id は rebuild を生き延び
ない」と明示していた（`focus_after_removing` の doc）。それでも
**production 化する task の plan と ADR のどちらも、その含意を持ち込む
側の問題として書いていない**。同じ phase で T4 は path（境界検査つき）を
選び、T5 は index（範囲外でパニック）を選んだ —— 2 つの保持識別子が
別々の寿命規約で並んだのに、どちらの task もその比較をしていない。

3 層目は**観測の切り分け方**である。フォアグラウンド獲得の失敗を
「機械の性質」と読んだのは、失敗の帰属先を**環境**に置いたからで、
手順の側を疑っていない。これは実装ゲートの罠 #6（症状を額面どおりに
受け取る）と同じ形が、コードではなく**道具**の側に出たものである。
罠 #6 は「決定的な失敗を再ロールして緑にするな」と書いてあり、私は
再ロールこそしなかったが、**逆向きに同じ誤りをした** —— 1 回の失敗を
根本原因まで追わずに環境の分類として確定させた。分類として書けば、
以後の task はその分類を前提に設計する。実際に危なかったのは T12 で、
「この機械では間欠的」を読んだ担当者は、**手順を尽くさないまま弱い証拠に
落ちることを正当化できてしまう**。

### 計画仮説・前提の問い直し（オーナーと）

- **メッセージをまたいで識別子を保持する task は、start gate でその識別子の
  寿命を明示する。** 「次に読むとき同じものを指す保証はあるか。無いなら
  範囲外になるのか、別ノードを指すのか」。T4（path）と T5（index）で
  答えが違っており、T7 / T9 は 3 回目に同じ問いを踏む。
- **純ロジックの core を「実装として採用する」決定は、その core の識別子
  契約も採用している。** ADR がそこに触れないなら、production 化する task
  が start gate で補うのが持ち主として自然である。
- **変異証人の限界を規律に書く。** 実装の変異は設計の穴を測れない。
  型で表現できる不変条件は型で表現する（今回 `path()` を `Option` に
  したのがそれ）方が、規律より強い。

### 反証可能な是正テスト

以後の task の **start gate に 1 行**を足す（T1 の「新しい store / 単位 /
座標系」、T2 の「消す性質を pin しているテスト」、T3 の「次の task に要求
する evidence を 1 回構築したか」、T4 の「依拠する否定的予測を 1 回測ったか」
に並べる）:

「**この task がメッセージ／フレームをまたいで保持する識別子は何か。
次に読むとき同じものを指す保証はあるか。無い場合、範囲外になるのか
別のものを指すのか、どちらの失敗になるかを書く。**」

反証条件は明確で、以後の task で「保持した識別子の寿命」起因の欠陥が
見つかったら、この是正は効いていない。

もう 1 つ、**close gate に 1 行**:「変異証人のうち、**実装の変異ではない
もの**は何本か。0 本なら、この task の証拠は『自分の実装を見ている』こと
しか示していない。」（W1 のような復元変異、または設計前提を破る変異が
これに当たる。）

3 つめ、**環境の能力を判定するときに 1 行**:「その能力を**取りに行く
手順を尽くしたか**。尽くす前に『環境に無い』と結論していないか。」
能力の不在は**分類として文書に残り、後続の task がそれを前提に設計する**
ので、誤った分類は 1 task の失敗では終わらない。反証条件は、環境の能力を
「無い」と分類した項目が、後で手順の側の不足だったと分かること。

### 後続タスク / オーナーへ共有すること

- **T7 は本 task から 3 件を引き継ぐ。** CF-T5-1（範囲内 index ずれ）、
  CF-T5-2（lazy な後継）、そして `discard_stale_focus` の去就 ——
  entry / exit が除去前に後継を計算するようになれば、この関数の理由は
  消える。`focus_spike` 撤去は、production の投影が既にあるので
  「書き換え」ではなく「削除」になった。
- **T8 は CF-T5-5。** キーの dispatch は traversal の消費と
  `DefWindowProc` 落としの**間**に入る。host のキー枠より前に入れると
  F5 が落ちる —— それが意図した仕掛け線である。
- **T12 は 2 件。** control B は Tab 駆動なので、キー入力の前に活性化を
  獲得・確認・再試行する手順（CF-T5-4、
  [verification-environments.md](../../../../docs/notes/verification-environments.md)
  Observation 4 に収録）を踏む。使った経路の記録も同じ規律の一部で、
  [plan.md](../implementation/plan.md) の T12 に明記した。
  [capture-t5-focus.ps1](../implementation/evidence/capture-t5-focus.ps1)
  がそのまま流用できる形である。加えて CF-T5-3 —— 表示の見た目を
  見られるのはフレームだけである。
- **T13 は CF-T5-6。** §13.3 の指標の 1 文を landed に合わせる（オーナー
  決着済み。[plan.md](../implementation/plan.md) の T13 確認項目に収録）。
  なお、文言修正は食い違いの解消であって制約の解消ではない —— 背景色しか
  手段が無い状況そのものは、[候補プール](../../../candidate-pool.md)の
  「枠線／囲み線によるフォーカスリング」の行が持つ。

## マージゲート

- **Review lane**（[implementation-gates.md §4](../../../procedures/implementation-gates.md)）:
  **full independent review**。preamble の予測どおりで、start gate で確認し
  close gate で再確認した —— 保持する per-window 状態、走査中核の最初の
  production 呼び出し元、`WM_KEYDOWN` の戻り経路の変更（runtime の構造変更）に
  加え、GUI 描画の証拠を持つ。罠 #4 の分岐/テスト観点は同じ review に合成。
- **実施状況**: 実装は subagent（Sonnet）が 3 段（production / fixture /
  stale-id 修正）で著作し、レビューは task lead が各段の差分に対して実施。
  subagent の報告のうち検証可能な主張は lead が独立に測り直した ——
  テスト総数は clean rebuild 後に再実行して確認、
  F5 の `LRESULT` 脚が非判別であることは lead の読解で検出して host の
  キー枠方式へ書き換え（その後 W7 で反証）、
  stale-id クラッシュは lead が自分の probe で独立に同じ panic
  （`focus.rs:63`, `len is 2 but the index is 4`）を測ってから修正を依頼した。
  変異証人 9 本のうち 8 本は lead が自分で入れ、**ファイルから読み戻して
  変異の存在を確認してから**走らせ、revert 後も再読して確認した。
  GUI 陽性対照は lead が実行（独立 2 回、全 8 フレームがビット一致）。
  **外部エージェント（codex）レビューはオーナー判断で実施しない**
  （T1〜T4 と同じ扱い。今回も同様でよいかは merge 承認時に確認する）。
- **merge**: `feat/m4-phase-2-t5` → `feat/m4-phase-2` を no-ff。
  **オーナー明示承認待ち。** push は別ゲート。
