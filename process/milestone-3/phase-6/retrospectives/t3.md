---
phase: M3-Phase 6
task: T3
title: ZStack runtime loader and Windows-runtime Visual evidence
date: 2026-06-03
scope: task-end
merge_target: feat/m3-phase-6
---

# T3 Retrospective

Task branch: `feat/m3-phase-6-t3`

Implementation commits:

- `63d6262` (`feat(wasamo-runtime): materialize ZStack loader visuals`)

## Checklist

1. Main learning: ZStack fit the existing runtime-widget pattern cleanly,
   but live Visual-order evidence needed direct `VisualCollection`
   enumeration, so the existing `windows` dependency had to enable
   `Foundation_Collections`.
2. Specification document changes: none. No `abi_spec.md`,
   `architecture.md`, or `dsl_spec.md` edits were needed for T3.
3. Post-commit verification (post-commit HEAD `63d6262`): green.
   - `cargo fmt --all -- --check`: green.
   - `cargo clean`: completed (`7195 files, 2.2GiB` removed).
   - `cargo build --release --workspace`: green. Existing Cargo warnings
     about the `wasamo` linkable target / `wasamo-sys` import-library
     ordering were observed.
   - `cargo build --workspace`: green, with the same existing warnings.
   - `cargo test --workspace`: green.
   - Additional scoped checks: `cargo test -p wasamo-runtime zstack`
     green; `cargo test -p wasamo-runtime` green.
4. Design judgments / trade-offs to consult PO: none. T3 follows
   DD-M3-P6-001 / DD-M3-P6-002 directly: direct ZStack children,
   parent-owned placement vector, outer-bounds clip on the ZStack Visual,
   no per-child clip, and no ABI change.
5. Out-of-task refactors or structural changes: none. The only dependency
   metadata change was enabling `windows` `Foundation_Collections` so the
   T3 live Visual-order fixture can enumerate the runtime child
   collection.
6. Need for an additional DD: none.
7. New Proposed ADR item or Proposed-to-Accepted promotion: none.
8. Milestone AC / phase-shape additions or changes: none.
9. Carry-over temporary implementation / approximations / new `dead_code`
   warnings: none. The T2 `LayoutNode::zstack` forward pointer is now
   wired through `WidgetData::ZStack` → `LayoutNode::zstack`.
10. New cross-task / cross-phase design constraints: none. The
    `Foundation_Collections` feature is a local test-read seam for this
    task's live Visual-order evidence, not a future author/runtime
    semantic constraint.
11. Follow-on task-list review: no revision needed. T7/T8 still own the
    real-pixel paint-precedence observation on the lightbox, and T4/T5/T6
    still own conditional rendering and static title wiring.

## Evidence Pointers

- Runtime validate tests:
  `zstack_positive_control_validates_direct_children`,
  `zstack_attribute_rejected_at_validate`,
  `zstack_binding_rejected_at_validate`,
  `zstack_child_unknown_alignment_rejected_at_validate`,
  `placement_prop_outside_zstack_child_or_grid_cell_rejected_at_validate`,
  and `validate_rejects_zstack_with_kind_payload`.
- ZStack emit -> parse loader roundtrip:
  `zstack_emit_then_parse_preserves_direct_children_and_order`.
- Mock-free Windows runtime Visual fixtures:
  `zstack_rooted_fixture_preserves_live_visual_order_and_clip` and
  `zstack_vstack_root_fixture_pins_production_root_shape`.
- Skip-guard disposition recorded in
  `process/milestone-3/phase-6/implementation/log.md`: no new runtime
  capability path; reused the Phase 5 `init_runtime_or_skip` pattern.

## Merge Gate

This retrospective completes the task-end checklist only. The merge from
`feat/m3-phase-6-t3` to `feat/m3-phase-6` still requires explicit owner
approval and must be no-ff per `process/procedures/retrospectives.md`.
