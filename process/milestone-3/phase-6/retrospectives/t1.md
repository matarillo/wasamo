---
phase: M3-Phase 6
task: T1
title: ZStack wasamoc check surface and IR emit
date: 2026-06-02
scope: task-end
merge_target: feat/m3-phase-6
---

# T1 Retrospective

Task branch: `feat/m3-phase-6-t1`

Implementation commits:

- `1f0688a` (`feat(wasamoc): add ZStack check surface and IR emit`)
- `feab4fd` (`fix(wasamoc): clarify ZStack placement diagnostics`)

## Checklist

1. Main learning: ZStack could stay on the generic `IrNode` lowering and
   textual-emit path, but check-side validation needed one extra parent
   context so `h-align` / `v-align` can be consumed by the ZStack parent
   before the child widget's normal prop validation.
2. Specification document changes: none. No `abi_spec.md`,
   `architecture.md`, or `dsl_spec.md` edits were needed for T1.
3. Post-commit verification (post-commit HEAD `feab4fd`): green.
   - `cargo fmt --all -- --check`: green.
   - `cargo clean`: completed (`2993 files, 1012.3MiB` removed).
   - `cargo build --release --workspace`: green. Existing Cargo warnings
     about the `wasamo` linkable target / `wasamo-sys` import-library
     ordering were observed.
   - `cargo build --workspace`: green, with the same existing warnings.
   - `cargo test --workspace`: green.
   - Additional scoped checks: `cargo test -p wasamoc` green;
     `cargo clippy -p wasamoc` green.
4. Design judgments / trade-offs to consult PO: none. The alignment
   admission rule follows DD-M3-P6-001 / DD-M3-P6-002 directly: ZStack
   direct-child placement props are parent-owned and rejected elsewhere.
5. Out-of-task refactors or structural changes: none. Changes stayed in
   `wasamoc` check/lower/emit tests plus the mutable T1 plan entry.
6. Need for an additional DD: none.
7. New Proposed ADR item or Proposed-to-Accepted promotion: none.
8. Milestone AC / phase-shape additions or changes: none.
9. Carry-over temporary implementation / approximations / new
   `dead_code` warnings: none.
10. New cross-task / cross-phase design constraints: none. The only
    non-obvious invariant used here is already in DD-M3-P6-002: placement
    props are consumed by the parent context and admitted only on a
    ZStack direct child or Grid `Cell`.
11. Follow-on task-list review: no revision needed. T2 owns ZStack layout
    measure/arrange, T3 owns runtime loader / Visual / loader validation,
    T7 owns the gallery positive-control fixture, and T4/T5 own
    conditional rendering.

## Evidence Pointers

- ZStack check positive / negative tests:
  `zstack_known_widget_no_warning`,
  `zstack_direct_child_alignment_accepted`,
  `zstack_unknown_attribute_rejected`,
  `zstack_reserved_layering_attribute_rejected`,
  `zstack_grid_track_attribute_rejected`,
  `zstack_child_bad_alignment_value_rejected`, and
  `placement_attr_outside_zstack_child_or_cell_rejected`;
  review-follow-up regression test:
  `placement_attr_on_zstack_itself_rejected_with_container_position`.
- ZStack lower / emit roundtrip-shape tests:
  `zstack_lowers_as_direct_children_without_kind_payload` and
  `zstack_emitted_as_node_with_direct_children_in_order`.

## Merge Gate

This retrospective completes the task-end checklist only. The merge from
`feat/m3-phase-6-t1` to `feat/m3-phase-6` still requires explicit owner
approval and must be no-ff per `process/procedures/retrospectives.md`.
