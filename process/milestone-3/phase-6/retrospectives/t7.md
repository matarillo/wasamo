---
phase: M3-Phase 6
task: T7
title: End-to-end gallery lightbox slice + assistant-side build / launch
date: 2026-06-07
scope: task-end
merge_target: feat/m3-phase-6
---

# T7 Retrospective

Task branch: `feat/m3-phase-6-t7` (to be merged to
`feat/m3-phase-6` after explicit owner approval).

Refs (branch `feat/m3-phase-6-t7`; hashes omitted — review-round amends
rewrite them):

- T7 gallery lightbox slice, root-ZStack validator fixes, assistant
  screenshot evidence (**codex** — `Co-Authored-By: codex`).
- T7 review follow-up: DD-M3-P6-008 (Proposed) + interim reject pins +
  plan/log/retro updates (**Claude**).
- gallery.ui reindent under the new root ZStack — whitespace-only (**Claude**).
- Review round 2: both-gate divergence pins
  (`zstack_root_component_window_attrs_accepted` accept side +
  `root_zstack_rejects_spliced_component_window_binding` faithful binding
  reject) + retro/plan corrections (**Claude**).

## Checklist (task-end, items 1-11)

1. **Main learning.** GUI evidence has two separable failure planes:
   runtime visibility and assistant capture. T7 initially looked like a
   rendering failure because `CopyFromScreen` failed and `MainWindowHandle`
   picked a helper HWND, but the owner-visible PID proved the Gallery window
   was actually displayed. The fix was to enumerate the launched process's
   visible top-level windows and match the `"Gallery"` HWND, then run the
   capture with GUI escalation. Separately, the first real launch attempts
   found two validator bugs: component-root window props spliced onto the
   root `IrNode` are not root widget attributes, and ZStack child placement
   props remain valid even when the direct child is another ZStack.
2. **Specification document changes:** none. The lightbox implements the
   accepted Phase 6 proof shape; the validator fixes align the runtime with
   the already-accepted ZStack / Window-title model rather than changing the
   public DSL, ABI, or architecture contract.
3. **Post-change verification:** green (recorded in
   [log.md](../implementation/log.md)).
   - `cargo fmt --all -- --check` — green.
   - `cargo clean` — completed (`3090 files, 1.1GiB` removed).
   - `cargo build --release --workspace` — green.
   - `cargo build --workspace` — green.
   - `cargo test --workspace` — green.
   - Scoped checks: `cargo run -p wasamoc -- check
     examples\gallery\gallery.ui`, `cargo build --release -p
     gallery-rust`, and `cargo test -p wasamo-runtime --lib zstack` —
     green.
   - Assistant GUI evidence: escalated
     `implementation/evidence/capture-lightbox.ps1` captured
     `t7-lightbox-closed.png`, `t7-lightbox-open.png`, and
     `t7-lightbox-closed-after-click.png`.
   - Existing Cargo warnings about the `wasamo` linkable target /
     `wasamo-sys` import-library ordering were observed.
4. **Design judgments / trade-offs to consult PO:** none. The owner already
   confirmed a visible Gallery PID during the capture/tooling investigation;
   T8 remains the explicit owner-visible smoke and fix container.
5. **Out-of-task refactors or structural changes:** one runtime-correctness
   fix folded in. The root-ZStack gallery shape could not launch until
   `validate_phase6_zstack_node_invariants` was corrected, so the fix is
   T7-owned, but it is a **correctness change, not a pure refactor**: it
   absorbed a **T3 validator coverage gap** (a ZStack direct child that is
   itself a ZStack could not carry `h-align` / `v-align`, though it is an
   ordinary direct child under DD-M3-P6-002). `wasamoc check` already
   accepted both that shape and the spliced component-root window props — the
   runtime validator was the only side wrong — which is the root cause of the
   divergence captured in item 10. No unrelated runtime path was refactored.
6. **Need for an additional DD:** **yes — DD-M3-P6-008 (Proposed)** records
   the component-root window-attribute / widget-attribute boundary (see item
   10). T7's allowlist fix is a localized unblock; the boundary itself is a
   design decision (A schema separation vs D compiler-owned catalog),
   deliberately left to that DD rather than silently patched. The remaining
   validator fixes are consistent with DD-M3-P6-001 / 002 and DD-M3-P6-006.
7. **Existing-ADR Proposed item / Proposed -> Accepted promotion:** none.
8. **Milestone-plan AC / phase-shape additions or changes:** none. T7 closes
   the assistant-side gallery evidence; T8 still owns the owner-manual GUI
   smoke and any visible-correctness fix.
9. **Carry-over temporary implementation / approximations / new
   `dead_code`:** none. The screenshot helper is task evidence tooling, not
   product code. No new `dead_code` warning was introduced.
10. **New cross-task / cross-phase design constraints:** **yes — escalated to
    DD-M3-P6-008 (Proposed), to resolve before Phase 6 closes.**
    - **Root cause (the deeper finding, not just the symptom):** component
      **window** attributes are spliced onto the **root widget's**
      `props` / `bindings` ([`wasamoc/src/lower.rs`](../../../../wasamoc/src/lower.rs#L59)).
      `wasamoc check` sees them as component-level (pre-splice AST) and passes
      *any* name; the runtime loader sees them as widget attributes
      (post-splice IR). Phase 6's ZStack validator is the first widget
      validator to strictly reject unknown props/bindings, so the leaky
      abstraction (window attrs masquerading as widget attrs) surfaced as a
      **dual-gate divergence** the moment the root became a ZStack.
    - **Scope is broader than props.** The same divergence applies to
      **bindings**: a component-level dynamic bind (`bind title = …`, FD-D,
      deferred) is spliced onto `root.bindings` and the ZStack root rejects
      all bindings — latent today, but the reason an allowlist-only patch is
      insufficient. T7's `{title, backdrop, theme}` allowlist is also
      **narrower than the compiler's accept set** (arbitrary component props),
      so a fourth window prop / a typo is accepted by `wasamoc` but rejected
      by a ZStack root.
    - **Interim pinned on both gates** (so a future alignment flips exactly
      one side, not silently both): the **accept** side in `wasamoc`
      (`zstack_root_component_window_attrs_accepted` — arbitrary prop + dynamic
      `title` bind pass `check` on a ZStack root) and the **reject** side in
      the runtime (`nested_zstack_rejects_component_window_prop`,
      `root_zstack_rejects_non_window_component_prop`,
      `root_zstack_rejects_placement_prop`,
      `root_zstack_accepts_component_window_props`,
      `root_zstack_still_rejects_widget_attribute`, and the binding facet
      `root_zstack_rejects_spliced_component_window_binding` — the exact
      `bind title = (str-prop-read s)` IR `wasamoc` emits, verified against
      `wasamoc build`).
    - **Placement:** not a passive `phase-sync` documentation item — it is a
      runtime-correctness divergence raised as **DD-M3-P6-008** and tracked at
      **plan.md T7b**, time-boxed to land before phase close (the realistic
      home is T7-surfaced / Phase-6-close, not a T6 reopen — T6 had no
      ZStack-root example to exercise it).
11. **Downstream-task revision:** **yes.** Added **T7b** (DD-M3-P6-008
    deliberation) to plan.md; added a **geometry positive-control** owner-
    observation to T8 (the assistant analysis confirmed z-order / dimming but
    not photo `aspect: 4:3` / centre-column width or caption-row fit). T9
    still owns Moment 2 implementation sync.

## Implementation-Gate Closure

- **#2 missed side effects:** the gallery slice wires `Open lightbox` and
  `x` through `root.is_lightbox_open`; the screenshot triplet proves present
  / absent without resize. The root `"Gallery"` title bar remains visible in
  the captured host.
- **#4 branch tests:** added `root_zstack_accepts_component_window_props`,
  `root_zstack_still_rejects_widget_attribute`, and
  `zstack_child_zstack_accepts_placement_props` for the newly-authored
  validator branches, plus the DD-M3-P6-008 **both-gate** divergence pins:
  accept side `zstack_root_component_window_attrs_accepted` (`wasamoc`);
  reject side `nested_zstack_rejects_component_window_prop`,
  `root_zstack_rejects_non_window_component_prop`,
  `root_zstack_rejects_placement_prop`, and the faithful binding pin
  `root_zstack_rejects_spliced_component_window_binding` (the exact IR
  `wasamoc` emits for a dynamic `title:`). The divergence is auditable from
  both gates until the DD is decided.
- **#5 carry-forward:** **one deferred design decision** — the component-root
  window-attribute / widget-attribute boundary (DD-M3-P6-008, plan.md T7b),
  re-trigger criterion = before Phase 6 closes. The concrete runtime behavior
  is pinned by the #4 tests; what is carried is the A-vs-D direction choice,
  not an unpinned invariant.
- **#6 deterministic-failure disposition:** the deterministic launch
  failures were root-caused to validator boundary mistakes and fixed with
  direct tests. The separate screenshot failure was isolated to sandboxed GUI
  capture and resolved by running the capture helper with GUI escalation.
- **#7 GUI evidence:** `t7-lightbox-closed.png`,
  `t7-lightbox-open.png`, and `t7-lightbox-closed-after-click.png` are the
  positive-control triplet. Analysis: closed shows the gallery with
  `Open lightbox`; open shows the scrim dimming thumbnails while the photo,
  caption, and nav paint over it; close-after-click shows the overlay gone
  immediately after the `x` click.
  - **Scope of this positive control (review-round-2 correction):** the
    screenshot's positive control is **z-order / dimming** — the scrim *dims*
    (does not replace) the thumbnails, which a flat opaque panel could not
    produce. It does **not** by itself prove *structural* present/absent: an
    always-built-but-hidden subtree toggled by opacity could produce the same
    triplet. The structural insertion / disposal and the `WindowState`
    dirty-layout path are proven by **T5's headless integration tests**
    (`conditional_toggle_preserves_declared_visual_order_and_disposes_registry`,
    registry destroy-count, fresh-Effect assertions); the screenshot
    corroborates them, it does not substitute for them.

Review lane remains **full independent review** before merge because this
task includes GUI-render evidence.

## Evidence Pointers

- Gallery source: `examples/gallery/gallery.ui`.
- Runtime validator: `validate_phase6_zstack_node_invariants`.
- Screenshot evidence:
  `process/milestone-3/phase-6/implementation/evidence/t7-lightbox-closed.png`,
  `process/milestone-3/phase-6/implementation/evidence/t7-lightbox-open.png`,
  and
  `process/milestone-3/phase-6/implementation/evidence/t7-lightbox-closed-after-click.png`.
- Verification index:
  [implementation/log.md](../implementation/log.md).

## Merge Readiness

Checklist complete; local verification and assistant GUI evidence are green.

**What T7 ships with a known interim:** the component-root window-attribute /
widget-attribute boundary stays at its reject-outside-allowlist interim,
pinned on both gates (item 10). The A-vs-D resolution is **DD-M3-P6-008
(Proposed)**, tracked at **plan.md T7b** and time-boxed before Phase 6 closes;
T8's owner smoke runs **after** T7b so it observes the final validator /
gallery behavior. DD-M3-P6-008 is committed as a **Proposed draft** (not
owner-approved) — its direction choice is open.

Per [retrospectives.md §進行手順](../../../procedures/retrospectives.md),
checklist completion is **not** merge authorization — the no-ff merge to
`feat/m3-phase-6` awaits explicit owner approval. Push remains a separate
phase-end gate.
