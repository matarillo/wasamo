---
phase: M3-Phase 6
task: T9
title: Phase-end gates and Moment 2 re-sync
date: 2026-06-08
scope: task-end
merge_target: feat/m3-phase-6
---

# T9 Retrospective

Task branch: T9 close work on `feat/m3-phase-6` (to be merged / retained on
the phase branch after explicit owner approval). This is the **step-end**
retrospective only; the separate phase-end retrospective (items 12-18) is
owned after T9 merges to the phase branch.

Refs:

- `30e0947` — T9 DSL spec Moment 2 sync for ZStack, conditional rendering,
  textual IR control-flow members, and component host surface.
- `803237d` — A12 diagnostic wording fix for direct conditional members under
  `ScrollView`; compiler / runtime tests assert the reader-facing message does
  not leak DD ids.
- `6089f54` — T9 architecture Moment 2 sync for ZStack, structural
  conditional mutation, and `IrComponent.host_props` / `host_bindings`.
- `8a41954` — T9 plan checklist state for the doc-sync / A12 / ABI /
  ADR-touch gates.
- Current T9 close docs — `implementation/log.md` evidence pointer and this
  retrospective.

## Checklist (task-end, items 1-11)

1. **Main learning.** T9 confirmed the Phase 5 timing correction is the right
   shape for final-step work: the last implementation step can record Moment 2
   sync, local evidence pointers, and step-end retrospective closure, but it
   must not pretend to own the phase-branch CI run id, handoff finalization,
   `implementation/preamble.md` status flip, or phase-end retrospective. The
   useful guardrail was naming those non-owned bullets explicitly instead of
   leaving the task list to imply "T9 closes everything."
2. **Specification document changes:** yes. T9 sync changed
   `docs/dsl_spec.md` and `docs/architecture.md` to the Phase 6
   implementation-synced state. `docs/abi_spec.md` was reconfirmed untouched:
   static Window title rides the existing `wasamo_load_ui` ->
   `window::create` path, and the `host_props` / `host_bindings` migration is
   internal compiler/textual-IR shape, not a C ABI surface. The ADR set was
   not substantively revised at T9 because none of the three permitted
   phase-sync ADR-touch cases applied.
3. **Post-change verification:** local green for the T9-owned A12 code change;
   CI run id / on-CI Windows evidence remain phase-end-owned.
   - A12 diagnostic follow-up: compiler / runtime tests for direct
     conditional members under `ScrollView` were tightened in `803237d` and
     passed at the scoped verification point recorded in
     [log.md](../implementation/log.md).
   - Post-A12 local step-end gate (T9-owned, recorded in
     [log.md](../implementation/log.md)): `cargo fmt --all -- --check` —
     green; `cargo clean` completed (`5699 files, 1.5GiB` removed);
     `cargo build --release --workspace` — green; `cargo build --workspace`
     — green; `cargo test --workspace` — green (`wasamo-ir` 18,
     `wasamo-runtime` 349, `wasamoc` 322, `wasamoc` roundtrip 6, all
     integration suites and doc-tests green).
   - Existing Cargo warnings about the `wasamo` linkable target /
     `wasamo-sys` import-library ordering were observed.
   - The phase-branch `workflow_dispatch` run id and on-CI Windows integration
     evidence remain phase-end-owned per [plan.md](../implementation/plan.md)
     T9 and stay unchecked at T9 step-close.
4. **Design judgments / trade-offs to consult PO:** none pending for T9. The
   owner already decided DD-M3-P6-007 (conservative `ScrollView` direct
   conditional rejection), DD-M3-P6-008 A2a (`host_props` / `host_bindings`),
   and the T9 non-ownership boundary for CI / handoff / phase-end work.
5. **Out-of-task refactors or structural changes:** none. T9's code change was
   the planned A12 diagnostic wording correction; the rest of the task is
   documentation synchronization and process evidence.
6. **Need for an additional DD:** none. The only T9 code follow-up kept the
   accepted DD-M3-P6-007 behavior unchanged and removed decision-id leakage
   from diagnostics; no new design option or policy change was introduced.
7. **Existing-ADR Proposed item / Proposed -> Accepted promotion:** none.
   DD-M3-P6-007 and DD-M3-P6-008 were already accepted before T9's close sync.
8. **Milestone-plan AC / phase-shape additions or changes:** none at
   step-close. The roadmap / milestone Phase 6 row `complete` flip is
   phase-end-owned, not T9-owned.
9. **Carry-over temporary implementation / approximations / new
   `dead_code`:** no new `dead_code`. T9 leaves no new implementation
   approximation, but intentionally leaves phase-end-owned checklist bullets
   open: CI gates / run id, `implementation/handoff.md`,
   `implementation/preamble.md` front-matter `active` -> `closing`, and
   `retrospectives/phase-end.md`.
10. **New cross-task / cross-phase design constraints:** **yes — classified at
    T9 step-close.**
    - **Constraint:** ZStack placement attributes (`h-align` / `v-align`) are
      parent-owned direct-child metadata; future placement-like surfaces must
      keep the compiler gate, layout helper/default semantics, and runtime
      validator in sync.
    - **Evidence:** T1/T2/T3 and the cross-task review follow-up pinned the
      check layer, layout defaults/helpers, validator gate, and live Visual
      offset evidence.
    - **Placement:** `doc-folded`. T9's `docs/dsl_spec.md` and
      `docs/architecture.md` sync describe the realised ZStack placement
      model and validation boundary.
    - **Constraint:** `IrMember` traversal must dispatch on widget vs
      control-flow members deliberately; widget-only helpers are valid only
      where dropping control-flow is the intended behavior.
    - **Evidence:** T4 review follow-ups fixed Box / ScrollView count gates,
      T5's reactive insertion needed declared-member slots, and T9's spec
      sync documented control-flow members as structural, not widgets.
    - **Placement:** `doc-folded` for the Phase 6 realised semantics in
      `docs/dsl_spec.md` / `docs/architecture.md`; the family-extension
      landing points for `else` / `switch` / iteration remain
      `carry-forward` for the phase-end handoff.
    - **Constraint:** Direct conditional members under `ScrollView` remain
      rejected in Phase 6; a future conditionally-empty ScrollView design must
      reopen the child-cardinality policy explicitly rather than reusing the
      current `if` machinery by accident.
    - **Evidence:** DD-M3-P6-007 / T4b chose the conservative branch, T9 A12
      sync folded the reader-facing rule, and `803237d` tightened the compiler
      / runtime diagnostic tests.
    - **Placement:** `doc-folded` for Phase 6 behavior. The alternative
      conditionally-empty design is `local-only` for T9 because it is not an
      active Phase 7 obligation; it becomes relevant only if a later phase
      deliberately reopens ScrollView cardinality.
    - **Constraint:** structural conditional mutation must mark layout dirty
      and drain fresh subtree effects before the triggering setter returns.
    - **Evidence:** T5 integration tests prove present/absent mutation,
      declared Visual order, disposal, and same-drain effect observation; T7/T8
      corroborated the click-driven lightbox toggle without relying on resize.
    - **Placement:** `doc-folded` for the realised Phase 6 behavior in
      `docs/architecture.md` and `docs/dsl_spec.md`; the accepted
      reactive-drain deferrals remain `carry-forward` for phase-end handoff.
    - **Constraint:** component host attributes must stay separated from the
      content root; future host/base modeling may replace the carrier but must
      preserve the separation.
    - **Evidence:** T7b moved `title` / `backdrop` / `theme` to
      `IrComponent.host_props`, added `host_bindings`, rejected old
      root-squatted IR, and T9 folded the canonical textual-IR / architecture
      shape.
    - **Placement:** `doc-folded` in `docs/dsl_spec.md` and
      `docs/architecture.md`; future dynamic host bindings, base-name
      validation, or ABI-facing window descriptor work remains
      `carry-forward` for the phase-end handoff.
    - **Constraint:** the lightbox is a visual overlay, not a modal input
      blocker, and its caption row depends on current text metrics / logical
      pixels.
    - **Evidence:** T8 owner smoke observed background click-through while the
      lightbox was open, and the additive `32` -> `64` caption-row fix was
      required for caption/nav separation.
    - **Placement:** `carry-forward` for phase-end handoff as M4 input /
      modal focus and DPI/text-metrics material.
    - **Constraint:** the Observation 5 ScrollView integration teardown issue
      no longer has a remaining Phase 6 remediation carry-forward: the
      Compositor owning-thread marshal and the apartment keep-alive helper are
      both done / committed.
    - **Evidence:** `docs/notes/verification-environments.md` §Observation 5
      marks both remediation steps DONE; commits `5241f62` (owning-thread
      marshal) and `3ace730` (keep-alive helper) are HEAD ancestors; and the
      Windows integration fixtures now run through
      `run_on_owning_runtime_thread_or_skip`.
    - **Placement:** `local-only` for T9 / phase-end handoff because there is
      no open Observation 5 investigation or remediation item left to carry.
      Future uses of the owning-thread + message-pump helper are ordinary M4
      test-harness input, not a Phase 6 residual.
11. **Downstream-task revision:** no T9 plan revision needed. The mutable plan
    already marks CI gates, handoff, `implementation/preamble.md` status flip,
    and phase-end retrospective as **not T9-owned**. The T9 retrospective
    bullet can be checked after this file lands; the phase-end retrospective
    bullet remains unchecked until the phase-branch phase-end commit.

## Implementation-Gate Closure

- **#4 branch tests:** T9 added no new accept/reject set, but it changed a
  diagnostic wording branch for direct conditional members under `ScrollView`.
  The close artifact is `803237d`, whose compiler / runtime tests fire the
  branch and assert the diagnostic no longer includes the DD id.
- **#5 carry-forward:** item 10 classifies the step retro's phase-sync
  material. Folded items are marked `doc-folded`; remaining forward material is
  explicitly phase-end-handoff-owned rather than hidden in T9.
- **#6 deterministic-failure disposition:** no recurring or vanished failure
  occurred during T9 close. The local post-A12 gate is green; only the
  phase-branch CI run id / on-CI Windows evidence is intentionally deferred to
  the phase-end batch.

Review lane: owner review-before-commit for the `dsl_spec` and `architecture`
sync concerns, plus a branch/test-focused check for the diagnostic wording
change. No new schema / runtime-structural / GUI-render evidence lane is
triggered by T9.

## Evidence Pointers

- Implementation plan: [plan.md](../implementation/plan.md) T9.
- Verification / evidence index: [implementation/log.md](../implementation/log.md).
- Synced specifications: [docs/dsl_spec.md](../../../../docs/dsl_spec.md) and
  [docs/architecture.md](../../../../docs/architecture.md).
- ABI no-touch disposition: [plan.md](../implementation/plan.md) T9
  `docs/abi_spec.md` bullet.

## Merge Readiness

Checklist items 1-11 are recorded. Per
[retrospectives.md §進行手順](../../../procedures/retrospectives.md),
checklist completion is **not** merge authorization — the T9 step-close merge /
retention on `feat/m3-phase-6` awaits explicit owner approval. Phase-end CI,
handoff, preamble status flip, and `retrospectives/phase-end.md` remain
separate phase-end gates.
