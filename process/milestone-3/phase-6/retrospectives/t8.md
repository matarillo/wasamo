---
phase: M3-Phase 6
task: T8
title: Owner-manual GUI smoke and visible-correctness fix
date: 2026-06-08
scope: task-end
merge_target: feat/m3-phase-6
---

# T8 Retrospective

Task branch: `feat/m3-phase-6-t8` (to be merged to
`feat/m3-phase-6` after explicit owner approval).

Refs (branch `feat/m3-phase-6-t8`):

- T8 visible-correctness fix: lightbox caption row height `32` -> `64` in
  `examples/gallery/gallery.ui`, plus owner smoke / evidence records
  (**codex**).

## Checklist (task-end, items 1-11)

1. **Main learning.** The assistant screenshot precheck caught a real
   geometry polish issue that the original T7 evidence did not cover: the
   two-line caption was too close to the nav row. The owner smoke then
   confirmed the intended visual proof passed after the additive row-height
   fix. The owner also found a separate input-model residual: the scrim is a
   visual overlay, not a modal input blocker, so background buttons can still
   receive clicks through gaps in the lightbox content.
2. **Specification document changes:** none. The caption-row fix is an
   example-layout correction within the accepted lightbox proof. The
   background-click observation is already outside Phase 6 scope in the ADR
   preamble (`hit-testing / focus capture / modal focus trap` = M4 input), so
   no Phase 6 spec / architecture change is required here.
3. **Post-change verification:** green (recorded in
   [log.md](../implementation/log.md)).
   - `cargo fmt --all -- --check` — green.
   - `cargo clean` — completed (`5798 files, 1.5GiB` removed).
   - `cargo build --release --workspace` — green.
   - `cargo build --workspace` — green.
   - `cargo test --workspace` — green.
   - Scoped checks before the owner smoke: `cargo run -p wasamoc -- check
     examples\gallery\gallery.ui`, `cargo run -p wasamoc -- build
     examples\gallery\gallery.ui`, and `cargo build --release -p
     gallery-rust` — green.
   - Assistant precheck screenshots:
     `implementation/evidence/t8-precheck/t8-lightbox-closed.png`,
     `.../t8-lightbox-open.png`, and
     `.../t8-lightbox-closed-after-click.png`.
     These are **post-fix** screenshots generated through
     `capture-lightbox.ps1 -OutputPrefix t8-lightbox`.
   - Caption-fix positive control (`implementation/evidence/t8-caption-fix/`):
     `caption-row-32-open.png` (pre-fix, two-line caption crowding the nav row)
     vs `caption-row-64-open.png` (post-fix, cleared) — a same-scene before/after
     pair with only the caption Grid row changed.
   - Resize positive control (`implementation/evidence/t8-resize-positive-control/`):
     a SAME-SIZE open-vs-closed pair at 1399x993 (`scrim-open-resized.png` vs
     `scrim-closed-resized.png`, plus the 800x600 baseline `scrim-open.png` /
     `scrim-closed.png`). The open frame is uniformly dimmed across the whole
     enlarged viewport, so the scrim holds `Fill/Fill` on resize.
     **Correction recorded:** an earlier across-size read mistakenly flagged a
     scrim-refill defect; the same-size closed reference confirmed the scrim
     covers the grown viewport (pass). See
     [log.md](../implementation/log.md) "T8 evidence rebuild". False-alarm and
     owner-collaborative scratch frames were deleted.
   - Existing Cargo warnings about the `wasamo` linkable target /
     `wasamo-sys` import-library ordering were observed.
4. **Design judgments / trade-offs to consult PO:** none pending. The owner
   confirmed all requested visual-smoke items passed. The background-click
   observation is recorded below as M4 input carry-forward rather than a Phase
   6 fix request.
5. **Out-of-task refactors or structural changes:** none. The only product
   change is the example-gallery lightbox caption row height; no runtime,
   compiler, IR, or schema behavior changed.
6. **Need for an additional DD:** none. The input residual is already covered
   by the Phase 6 ADR preamble out-of-scope list and does not change the
   accepted ZStack / conditional / title decisions.
7. **Existing-ADR Proposed item / Proposed -> Accepted promotion:** none.
8. **Milestone-plan AC / phase-shape additions or changes:** none. T8 closes
   owner-visible smoke; T9 remains the phase-close and Moment 2 sync owner.
9. **Carry-over temporary implementation / approximations / new
   `dead_code`:** no new `dead_code`. The non-modal input behavior is a
   deliberate Phase 6 scope boundary, not a temporary implementation inside
   this task.
10. **New cross-task / cross-phase design constraints:** **yes —
    `carry-forward` candidate.**
    - **Constraint:** the Phase 6 lightbox is only a visual overlay; M4 input
      / modal design must decide whether an open lightbox should block
      background hit-testing and focus, and if so implement event capture /
      dispatch suppression for the scrim or modal subtree.
    - **Evidence:** owner T8 smoke passed the requested visual checks but
      observed that clicking through gaps between photo / caption / nav can
      activate the underlying `Scroll up` / `Scroll down` buttons and move the
      ScrollView while the lightbox remains open.
    - **Placement:** `carry-forward` candidate for phase-end retrospective
      item 15. The ADR preamble already marks `hit-testing / focus capture /
      modal focus trap` as M4 input scope; the phase-end handoff should carry
      this concrete owner observation so M4 treats it as an observed behavior,
      not just an abstract out-of-scope line.
    - **Constraint:** the lightbox caption-row fix is coupled to the current
      text metrics / logical-pixel behavior: `64` logical px is enough for the
      current two-line caption plus spacing, but M4 DPI-awareness or font /
      text-metric changes must re-check that the caption still clears the nav
      row.
    - **Evidence:** T8 changed the caption row from `32` to `64` after the
      assistant screenshot precheck showed the two caption lines too close to
      the nav row; owner smoke confirmed the corrected geometry.
    - **Placement:** `carry-forward` candidate for phase-end retrospective
      item 15. Re-trigger criterion: any M4 work changing DPI handling,
      default text metrics, font sizing, Grid row semantics, or lightbox copy /
      nav layout must re-check caption/nav separation.
11. **Downstream-task revision:** no plan revision needed. T9 already owns
    phase-close doc sync and handoff consolidation; this T8 carry-forward
    candidate can be resolved there.

## Implementation-Gate Closure

- **#2 missed side effects:** the visible fix changes only the lightbox Grid
  row allocation for caption spacing. It does not affect the conditional
  toggle, ZStack child order, scrim fill, title path, runtime loader, or IR.
- **#4 branch tests:** not applicable after implementation; no diagnostic,
  reject, size, or layout branch was added.
- **#5 carry-forward:** item 10 records two carry-forward candidates: the
  owner-observed background-click behavior (re-trigger: M4 work on
  hit-testing, focus capture, modal overlay behavior, Button event dispatch,
  or lightbox input semantics) and the caption-row metric dependency
  (re-trigger: M4 DPI / text metrics / Grid row semantics / lightbox copy or
  nav layout changes).
- **#6 deterministic-failure disposition:** no recurring or vanished runtime
  failure occurred. The assistant's precheck observation was a stable geometry
  issue and was fixed additively before owner smoke. The resize observation was
  reproducible; the apparent scrim-refill defect was an across-size reading
  error, corrected with a same-size positive control (see #7 / log.md).
- **#7 GUI evidence:** the positive-control evidence is now committed assistant
  same-size pairs, not single frames: caption fix = `t8-caption-fix/`
  (row 32 vs 64 open), resize = `t8-resize-positive-control/` (open vs closed at
  1399x993, plus 800x600 baseline). Owner manual smoke independently confirmed
  closed->open->close toggle, z-order / dimming, full-viewport scrim on resize,
  `"Gallery"` title, 4:3 photo geometry, and caption/nav non-overlap.
  **Method note:** the scrim alpha is subtle, so the resize control must compare
  open vs closed at the **same** size; an across-size comparison initially
  produced a false scrim-refill defect that the same-size reference overturned.

Review lane remains **full independent review** before merge because this
task closes GUI-render evidence and includes an example-visible fix.

## Evidence Pointers

- Gallery source: `examples/gallery/gallery.ui`.
- Screenshot evidence:
  `process/milestone-3/phase-6/implementation/evidence/t8-precheck/`.
- Capture helper:
  `process/milestone-3/phase-6/implementation/evidence/capture-lightbox.ps1`
  with `-OutputPrefix t8-lightbox`.
- Verification index:
  [implementation/log.md](../implementation/log.md).

## Merge Readiness

Checklist complete; owner-visible smoke and local fmt / clean rebuild /
workspace tests are green. Per
[retrospectives.md §進行手順](../../../procedures/retrospectives.md),
checklist completion is **not** merge authorization — the no-ff merge to
`feat/m3-phase-6` awaits explicit owner approval. Push remains a separate
phase-end gate.
