# M3 Phase 8 T1 retrospective

Date: 2026-07-03
Branch: `feat/m3-phase-8-t1`
Task commit: `e2ed342` (`process(m3-phase-8): close T1 pre-implementation spike`)

## Task-end checklist

1. **What changed?** T1 was tightened from a broad "pre-implementation spike"
   into a source-grounded responsibility cut: repair the task boundary,
   record start-gate trap selection, audit the current source touch points,
   and leave implementation choices with their owning tasks. No production
   source code was changed.
2. **Spec/doc sync needed?** No normative `docs/` spec update was warranted.
   The phase implementation docs were updated instead:
   `implementation/plan.md`, `implementation/preamble.md`, and
   `implementation/log.md`.
3. **Post-commit verification:** run on commit `e2ed342`.
   - `git diff --check` -> passed.
   - `cargo fmt --all -- --check` -> passed.
   - `cargo clean` -> removed prior build artifacts.
   - `cargo build --release --workspace` -> passed; existing Cargo warnings
     about the `wasamo` linkable target and initial `wasamo-sys` import
     library ordering were observed.
   - `cargo build --workspace` -> passed with the same existing warnings.
   - `cargo test --workspace` -> passed.
4. **Owner / PO consultation needed?** No new owner decision was made in T1.
   T1 explicitly left the A1 placeholder agreement, SI-1 visual ambiguity
   checkpoint, G(1), and beta fallback judgments with their later owning
   tasks/checkpoints.
5. **Out-of-scope refactor or cleanup?** None. The throwaway wrong-kind probe
   was deleted before commit.
6. **New decision record needed?** No. T1 did not change the accepted
   author-facing `ToggleButton` surface or phase policy.
7. **ADR status changes?** None.
8. **Acceptance criteria / phase structure change?** No milestone acceptance
   criterion changed. The T1 checklist and risk table were refined so the
   phase plan records what the source audit actually proved.
9. **Temporary code, approximation, or `dead_code` left behind?** None in
   production files. Carry-forward items were recorded in the implementation
   log rather than encoded as code.
10. **New cross-task or cross-phase constraint?** Yes: the widget kind is
    currently string-carried and unknown widget types are warnings with exit
    0, so future new-widget tasks must include positive "known widget"
    fixtures instead of relying on check success alone. T3 owns the first
    application of that constraint for `ToggleButton`.
11. **Task list review:** Done. T1 remains a single task, but its checklist was
    revised to include the responsibility buckets, the T3 gate selection, and
    the source-grounded risk sharpening. The default sequence
    T2 -> T3 -> T4 -> T5 -> T6 -> T7 still stands.

## Double-loop retrospective

- **Observation:** The original plan assumed a compiler-error-driven landing
  audit would enumerate `ToggleButton` touch points. The source audit and
  wrong-kind probe falsified that: the IR kind is a string, and
  `wasamoc check` accepts an unknown widget with a warning.
- **Underlying lesson:** For string-carried surfaces, "builds" is not enough
  evidence that a new kind was admitted. The positive-control fixture has to
  prove the intended catalog entry is recognized.
- **Plan correction:** T1 recorded the limitation in `implementation/log.md`
  and sharpened R-1 in `implementation/preamble.md`; T3 now starts with a
  known-widget/no-warning fixture obligation.
- **Falsifiable follow-up:** At T3 close, a reviewer should be able to point
  to fixtures that fail before `ToggleButton` is added and pass afterward,
  including evidence that no unknown-widget warning remains.

## Merge note

Task retrospective is complete for the branch-to-phase merge gate. The actual
merge still requires explicit owner approval after the planned independent
review.
