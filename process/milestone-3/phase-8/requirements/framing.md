---
title: M3-Phase 8 フレーミング — 選択中状態 ＋ ギャラリー統合 ＋ DSL 仕様書の公開ドラフト
status: aligned
created: 2026-06-25
target-phase: M3-Phase 8
---

# M3-Phase 8 フレーミング

**状態:** aligned（オーナー合意済み 2026-06-25）
**対象フェーズ:** M3-Phase 8（選択中状態 ＋ ギャラリー統合 ＋ DSL 仕様書の
公開ドラフト）— **M3 の最終フェーズ**

開発プロセス（[workflow.md §2](../../../procedures/workflow.md)）に従い、本
ドキュメントは設計判断（ADR）を書く前に **オーナーと方向性を合意する**ための
入力資料です。個々の判断の選択肢・比較・推奨は、ここでは確定しません。ここで
固めるのは、Phase 8 は何を届けるフェーズなのか、どの問いを判断項目（DD）として
予約するのか、どこまでを Phase 8 の担当範囲とし、どこからを「きっかけ（trigger）
が来たら後続フェーズで」と送るのか、そして検証のしかたです。

前フェーズからの制約引き継ぎ（プロセスの §2.1）は
[constraints.md](./constraints.md) で済んでおり、本ドキュメントはその上に
乗ります。先行する M3 各フェーズから受け継ぐ作業規律（仕様同期を 2 回に分ける /
「まとまり（Moment）」はコミット単位ではない / 全マージはオーナー承認を要する /
最終タスクと締めの責務を分ける / 実装ゲート / 陽性対照）はここで再説明せず、
[constraints.md §9](./constraints.md) を参照します。

> **用語の最小限の補足（初出のみ）:**
> - **DD（判断項目）** … このフェーズで決めるべき設計上の問い。1 つの DD が
>   1 つの問いに対応し、後で ADR（決定記録）として書く。
> - **公開ドラフト** … `docs/dsl_spec.md`（DSL の仕様書）を、社外の読者や
>   将来のツール開発が依拠できる「最初の公開版」として出すこと。
> - **配置（placement）** … 「子をこの親の中でどう置くか」（行・列・寄せ方）の
>   情報。前フェーズ 7b で書き方が確定済み。

---

## 今回オーナーに決めてほしいこと（Owner alignment packet）

**この節だけ読めば合意判断できます。** 推奨でよければ「OK」、変えたい項目
だけ指示してください。各項目は合意後に右端の **確定先** へ転記します
（1 項目 ＝ 1 合意単位 ＝ 1 確定先）。詳しい根拠は後続の各節に置きます。

| 記号 | 決めてほしいこと | 推奨 | 確定先 |
| -- | -- | -- | -- |
| A | Phase 8 の目的 | M3 を閉じる**締めくくりフェーズ**。届けるのは 3 つ — (i) ボタンの**選択中状態**の書き方を確定してギャラリーのタブ風セクション（または最小の実証手段）で選択中状態を動かす、(ii) これまで各フェーズで作った画面を 1 つの**ギャラリーに統合**し 3 言語（C / Rust / Zig）で通しで動かす、(iii) DSL 仕様書を**公開ドラフト**に昇格する。**「統合」とは、[gallery-wireframe.html](../../requirements/gallery-wireframe.html) の Photo Gallery という 1 つのターゲットアプリの中で M3 機能が全部通ること**を指す（検証画面の寄せ集めやデモメニューを作ることではない）。新しい見た目の機能は (i) だけで、残りは**統合と編集**。新しいレイアウト部品は足さない。配置の書き方は 7b で凍結済みなので**読むだけで作り直さない**。**framing で確定するのは目的・スコープ・確認ゲートまで**で、どの画面にどの機能を割り当てるかの最終 UI 詳細は G(1) / DD-001 / 実装計画で確定する。 | FD-8-A |
| B | 判断項目（DD）の構成 | **2 本**。**DD-M3-P8-001** ＝ ボタン選択中状態の書き方（選択肢: `selected: bool` 属性 / 独立部品 `ToggleButton` / 見た目テーマ経由）。**DD-M3-P8-002** ＝ 公開ドラフト昇格時の**「予約」と「未確定部分の見せ方」**（M4 向け構文を予約するか、暫定の項目をどう書くか、版マーカーと社外読者チェックの合格線）。ギャラリー統合は実装・検証作業なので DD にしない。 | FD-8-B（選択肢の詳細は §論点一覧） |
| C | 選択中状態の方向性 | **`selected: bool` 属性**を推奨。既存の真偽値バインディング（Phase 1）にそのまま乗り、新しい部品を要さず、ギャラリーのタブ風セクション（複数ボタンのうち 1 つが選択中）を**動かす候補にできる**（A10 の実証手段はスパイクと DD-001 で確定。現状は `==` も host state 書き込みも無いので、実現性は手書きブロック代入か単一トグルに依存）。見た目の変化は M3 では**最小限**（色や枠の違い）に留め、本格的なテーマは M5。「1 つだけ選択中」という排他は**新グループ部品を作らず**、出荷済みサーフェスで表現できるかを**スパイクで確認**して決める（ホスト state 書き込み経路は M3 に無いため『ホスト側ロジック』は不可。手書きブロック代入で足りるか、A10 の実証手段を単一トグルにするかを含め DD-001 で確定）。**適用対象（タブが中心候補。wireframe はサムネイル強調も選択中状態として持つ）と最小の見た目合格線（背景色だけ / 枠線だけ / 色＋枠）は DD-001 で owner に提示して確定**。結論は DD-001 で確定。 | FD-8-C（詳細は §論点一覧） |
| D | 明示サイズ問題（Problem B）の扱い | Phase 8 の方向づけ段階で **マイルストーン横断の決定記録（Vision DR）を起票**するが、これはロードマップに関わる全体方針なので **M3 の完了をブロックしない**。Phase 8 の責務は「起票して論点を固定する」「公開ドラフトでサイズ指定を**未確定の将来機能**として明記する」までで、**実装も最終確定も別のゲート**。フェーズの判断項目（DD）には含めない。 | FD-8-D ＋ Problem B Vision DR（cross-milestone/decisions/） |
| E | 担当範囲と線引き | 含む ＝ 選択中状態の実装一式（構文解析・検査・変換・実行時・例の同期）、ギャラリー統合（3 言語）、各フェーズ用の**使い捨て検証画面の片付け**、仕様書の編集。**余計な検証メニュー / デモ画面 / 新レイアウトは足さず、使い捨て検証画面を Photo Gallery ターゲットアプリへ畳み込む**（ただし A1 の陽性対照に必要な**最小操作 UI**＝状態切替ボタン等は、wireframe の範囲内または owner 合意済みの仮置きとして許容する。「余計」とは検証専用メニュー / dashboard を指す）。含まない ＝ M4 以降の機能（実画像 / 入力 / モーダル / テーマ等）と [constraints.md 末尾表](./constraints.md)の前送り維持項目。新しいレイアウト部品・新しい寸法計算は作らない。 | FD-8-E |
| F | 新しい受入れ基準は要るか | **不要の見込み**。3 つの成果はいずれも既存の受入れ基準（A1 / A10 / A12）が既に名指している（7b で A13 を新設したのとは違い、選択中状態は最初から A10 が予約済み）。DD-002 が**新しい公開上の約束を増やさない限り**「新基準なし」を計画の改訂ログに記録する。増やす場合のみ基準改訂の例外手続き。 | §Phase 8 受入れ基準 ＋ 計画 Revision-log |
| G | オーナー向け確認ポイント（UI チェックポイント） | Phase 8 の実装計画に、(1) **wireframe の忠実再現箇所 / M3 仮置き許容箇所の合意**（初回表示の前提として先に行い、結果を A1 機能対応表へ反映）、(2) 統合ギャラリーが代表 1 言語で**初めて表示できた時点**の UI 確認、(3) 選択中状態 / lightbox の**陽性対照**（切り替え前後 2 コマ）の確認、(4) **公開ドラフト ＋ M3 handoff** ドラフトの確認、(5) 最終の人間目視スモーク、の 5 つを**工程として明記**する。phase 末だけに寄せない。`gallery.ui` は wireframe の Photo Gallery を可能な限り忠実に再現し、余計な検証 UI を残さない。 | FD-8-G ＋ 実装計画 |

**返事チェックリスト:**

* A 目的: ☑
* B 判断項目は 2 本: ☑
* C 選択中状態は `selected: bool` 方向: ☑
* D 明示サイズ問題は Vision DR 起票のみ（M3 完了を止めない）: ☑
* E 担当範囲（実装まで＋線引き）: ☑
* F 新基準は不要見込み（DD-002 次第）: ☑
* G オーナー向け UI 確認ポイントを工程化: ☑

**packet の読み方（結論の先取りを避ける）:**

* **C の OK は方向合意であって代替案の却下ではない。** packet C への OK は
  DD-001 を `selected: bool` 推奨で**ドラフトする**合意であり、DD-001 の比較・
  owner accept なしに `ToggleButton` / theme 経由 / 将来のグループ意味論を
  却下するものではない（将来互換は DD-001 §Out of scope で明示する）。
* **checkpoint は framing/ADR へ戻せる。** FD-8-G の各 checkpoint の結果、
  初期仮説を超える設計影響が出た場合は、実装計画の更新だけでなく該当 DD /
  framing の小改訂に戻す。

---

## オーナー合意の記録

**2026-06-25**: owner が Owner alignment packet A〜G に正式合意（**推奨案を
すべて受け入れ**）。A→FD-8-A、B→FD-8-B、C→FD-8-C、D→FD-8-D、E→FD-8-E、
F→§Phase 8 受入れ基準＋plan Revision-log、G→FD-8-G＋実装計画 に確定。

合意に至る経緯: framing draft に対し Codex の複数ラウンドのレビューを批判的に
検討して folding し、(i) **gallery 統合 = Photo Gallery ターゲットアプリへの
畳み込み**（検証画面集 / dashboard ではない）を packet A/E・受入れ基準・検証
方針に明記、(ii) **owner UI 確認を FD-8-G として工程化**（初回表示 / 陽性対照 /
公開ドラフト＋handoff / 最終スモーク / wireframe 逸脱合意）、(iii) **A1 機能
対応表を初期仮説扱い**（FD-8-G(1) と DD-001 で更新され得ると明記）、(iv) **選択
中状態の排他の実現性**を出荷済みサーフェスの事実（式に `==` が無い・handler は
状態へのブロック代入のみ・表示中に host が state を書く API が無い）に基づいて
**事前スパイク（DD-001 採択前）へ戻し**、DD-001 の選択肢を案α/β/γ に立て直し
（旧「ホスト側ロジック vs グループ概念」は廃止）、(v) **`selected` 伝播監査 /
レイアウト骨格の技術 smoke** をリスク・検証方針に追加、(vi) わかりにくい英語語彙
（vehicle 等）を日本語化、まで反映済み。

各 packet の確定値（推奨どおり）: A=締めくくりフェーズ（A10 + A1 + A12、新
primitive なし）、B=DD 2 本（DD-M3-P8-001 選択中状態 / DD-M3-P8-002 公開ドラフト
昇格）、C=`selected: bool`（draft 方向。代替案は DD-001 で却下せず。実証手段と
排他はスパイクで確定）、D=Problem B は Vision DR 起票のみで M3 完了を止めない、
E=実装まで＋検証画面の片付け・M4 以降は前送り、F=新 AC 不要見込み（DD-002 が
公開上の約束を増やさない限り）、G=owner UI 確認の工程化。

**2026-07-03 追記（DD-001 Accepted 後の確定記録。packet 本文は当時記録として
保持し、上書きしない）:** packet C が推奨した `selected: bool` 属性は、DD-001 で
**B1（普通の Button への裸の属性）として却下**され、採択形は **T1 —
専用部品 `ToggleButton { checked: <bool> }`**（controlled one-way・V-a 背景色）に
確定した（[DD-M3-P8-001](../decisions/dd-m3-p8-001-button-selected-state-surface.md)）。
これは「packet の読み方」で予告したとおり、packet C の OK が**方向合意**であって
代替案の却下ではなく、具体形は事前スパイク ＋ DD-001 の比較で仕分ける、という
運用の結果である。よって `_roadmap.md` / `plan.md` の A1 / A9 / A10 / A12 文言は
`ToggleButton` / `checked` の採択語彙へ再同期済み（DD-001 §Accepted disposition
item 6）。framing の受入れ基準（A10 が selected-state を予約）は不変。

---

## Phase 8 受入れ基準

正本（SSOT）は [process/_roadmap.md M3](../../../_roadmap.md#m3-dsl-surface) /
[plan.md §Acceptance criteria](../../plan.md)。Phase 8 は M3 の最終フェーズと
して、次の**既存の受入れ基準**を満たします。

* **A10** — ボタンの選択中状態の書き方。具体的にどう書くか（`selected: bool`
  属性 / 独立部品 `ToggleButton` / テーマ経由）は「それを担当するフェーズで
  確定」と計画に書かれており、**その担当が Phase 8**。DD-M3-P8-001 で確定。
* **A1** — 統合した `examples/gallery/gallery.ui` が M3 の機能をすべて
  「DSL → 中間表現（IR）→ 実行時」の経路で通しで動かす。Phase 8 は各フェーズが
  育てた画面を**統合**し、3 言語すべてで動かす。統合先は
  [gallery-wireframe.html](../../requirements/gallery-wireframe.html) の
  Photo Gallery ターゲットアプリで、[spec.md](../../requirements/spec.md) の
  同期ルールが「各フェーズの画面を通し証明として置き、**最終フェーズで full
  app として閉じる**」と定める。使い捨て検証画面は残さず畳み込む（dashboard
  ではない）。
* **A12** — `docs/dsl_spec.md` を公開ドラフトに昇格。Phase 1〜7 で各フェーズ
  ごとに更新済みなので、Phase 8 の作業は**編集**（ゼロから書くのではない）。
* **A11**（継続的な義務）— `.ui`（DSL）/ 中間表現 / コンパイラ（`wasamoc`）/
  実行時 / 仕様書 / ギャラリー例 が足並みをそろえて閉じる。Phase 8 でも適用。

**新基準が要らない理由（F の根拠）:** Phase 8 の 3 つの成果はいずれも既存の
基準が名指しています。7b では配置の*書き方*をどの基準も名指していなかったため
A13 を新設しましたが、選択中状態は最初から A10 が予約しています。よって
**新基準は不要**の見込み。例外は、DD-M3-P8-002 が「M4 構文の予約」などで
**新しい公開上の約束**を生む場合のみで、そのときだけ基準改訂の例外手続き
（改訂ログ ＋ ロードマップ反映）を踏みます。判断は ADR 確定（Accepted）時点で
固定し、計画の改訂ログに記録します。

**マイルストーン完了基準（Phase 8 ＝ M3 の締めが背負う）:**
[plan.md §Milestone-end criteria](../../plan.md) のとおり、A1〜A13 をすべて
満たす / 3 つの成果（A10・A1・A12）/ 変更履歴（CHANGELOG）/ 各フェーズの仕様
同期が監査可能 / **社外読者チェック**（仕様書だけで M3 の機能を再現できるか）/
**こっそり先送りした M3 機能が無いこと** / 統合後のクリーンビルドが CI で緑。
これらは Phase 8 の締め（§6）とマイルストーンの締め（§7）にまたがります。

---

## Phase 8 設計方針

方針は、**再検討しない前提**と、**判断項目（DD）で開く問い**に分けます。

### 再検討しない前提（確定済み）

1. **配置の書き方は 7b で凍結済み。** `slot.*` の書き方、Grid の `Cell` ＋
   直接 `slot.*`（PM-2）、子スロットでの保持（`SlotData`）は
   [docs/dsl_spec.md](../../../../docs/dsl_spec.md) §4.16 /
   [docs/architecture.md](../../../../docs/architecture.md) §6.8.6 / §6.8.4
   に同期済み。Phase 8 は**確定済みの記述を読んで編集で磨く**だけで、書き方を
   作り直しません（[constraints.md §1](./constraints.md)）。
2. **新しいレイアウト部品・寸法計算は作らない。** Grid / WrapPanel / ZStack /
   ScrollView / Box は出そろっています。Phase 8 は統合と編集のフェーズ。
3. **汎用値型（TypedValue）は引き続き先送り。** Phase 7 で「採用しない」と
   判断済みで、Phase 8 の選択中状態は既存の真偽値（`bool`）に乗るため、新たな
   型の必要性は生じません
   （[typed-value-evaluator.md](../../../../docs/notes/typed-value-evaluator.md)）。

### 判断項目（DD）で開く問い

4. **A10 の「選択中」をどう書くか。** 真偽値バインディング（Phase 1）が
   ウィジェットの*属性*を動かせることを示す、M3 最後の機能。`selected: bool`
   属性が自然ですが、独立部品 `ToggleButton`・テーマ経由との比較は
   DD-M3-P8-001 で行います。なお「1 つだけ選択中」の排他は、式に比較演算子が
   無い等の制約があるため、**出荷済みサーフェスで表現できるかをスパイクで確認**
   したうえで、DD-001 が実証手段（タブ帯か単一トグルか）と表現法を決めます。
5. **公開ドラフトで「何を確定として書き、何を未確定として書くか」。** A12 は
   編集作業ですが、最初の公開ドラフトは社外の読者が依拠し始める**最初の公開
   契約**です。後で変えると破壊的変更になるので、未確定の将来機能
   （PM-2 のラッパー規則・サイズ指定・既定の寄せ方の非対称・綴り）を「確定」と
   誤読させない見せ方が要ります。M4 構文を予約するか否かも計画が「明示的に
   決めよ」と求めています。これらを DD-M3-P8-002 で扱います。

この方針は M3 の機能の幅を広げるものではありません。むしろ、**公開する前に
書き方の見せ方を確定し、未確定の部分を正直に未確定と書く**ための締めくくりです。

---

## notes の棚卸し（Phase 8 で検討するオープンクエスチョン）

[workflow.md §1.1](../../../procedures/workflow.md) の「探索メモ点検」を
Phase 8 にも適用し、`docs/notes/` の再評価きっかけ（trigger）のうち Phase 8 で
発火するものを確認します。

| 探索メモ | Phase 8 で発火するか | Phase 8 の扱い |
|---|---|---|
| [author-controllable-sizing.md](../../../../docs/notes/author-controllable-sizing.md)（明示サイズ ＝ Problem B） | **発火する**（§7.2: 起票タイミング ＝ Phase 8 の方向づけ段階） | **Vision DR を起票**（packet D）。公開ドラフトでサイズ指定を未確定の将来機能として明記（DD-002）。`aspect` を Grid セル内で使ったときの中断も同じ仕分けに含める。**確定は急がない**（マイルストーン横断）。 |
| [architectural-family.md](../../../../docs/notes/architectural-family.md)（DSL の系統） | **発火する**（きっかけ 1 ＝ M3 の仕様策定。公開ドラフトがその総仕上げ） | 系統への影響を再確認。Phase 8 は選択中状態（木構造を記述する真偽値属性）以外に新しい文法を入れず、画面を再実行するような方式も導入しないため、**現系統 (1) の範囲内で確認**できる見込み。記録は「その場で更新」（重い決定記録は不要、方針転換級のときのみ起こす）。 |
| [typed-value-evaluator.md](../../../../docs/notes/typed-value-evaluator.md)（汎用値型） | 発火しない | 選択中状態は既存の `bool` に乗る。新たな型の圧力なし。**先送り維持**。 |
| [component-extension-model.md](../../../../docs/notes/component-extension-model.md) / [m4-interaction-intake.md](../../../../docs/notes/m4-interaction-intake.md) / [top-layer-overlays.md](../../../../docs/notes/top-layer-overlays.md) / [layout-engine.md](../../../../docs/notes/layout-engine.md) | 発火しない（M4 / M5 担当） | コンポーネント ＝ M5、入力 / モーダル / オーバーレイ挙動 ＝ M4。Phase 8 のギャラリーは仮置き規律（Box ＋ Text ＋ 素のボタン）で構造の経路だけ証明し、これらには触れない。参照のみ。 |
| [verification-environments.md](../../../../docs/notes/verification-environments.md) / [human-visible-smoke.md](../../../../docs/notes/human-visible-smoke.md) / [headless-verification.md](../../../../docs/notes/headless-verification.md) | 規範として常時適用 | A10 / A1 の目視証拠（スクリーンショット ＋ 解析 ＋ 陽性対照）とオーナー目視確認の正本。§検証方針で参照（再説明しない）。 |

---

## 論点一覧（DD slate: 番号予約のみ）

本フェーズの決定記録（`decisions/preamble.md` ＋ 判断項目ごとに 1 ファイル）が
担う問いを列挙し、**DD-M3-P8-NNN 番号を予約**します。各判断項目の選択肢・比較・
推奨はプロセス §3（設計判断）で書きます。ここでは「何を判断するか」と「なぜ
Phase 8 の問いか」だけを固定します。

### DD-M3-P8-001 — ボタン「選択中」状態の書き方

**問い:**
作者はボタンの「選択中（selected）」状態を `.ui` でどう書くべきか。

**Phase 8 の問いである理由:**
A10 を満たす**唯一の新しい書き方**です。真偽値バインディング（Phase 1）が
ウィジェットの*属性*を動かせることを示す M3 最後のピースで、ギャラリーの
タブ風セクション（複数ボタンのうち 1 つが選択中）を**動かす候補**です（実際に
タブ帯で動かすか単一トグルにするかは、排他の実現性を見る 事前スパイクと
DD-001 で確定）。`selected` は仕様書にもギャラリーにも未登場のため、ここで
初めて書き方を定義します。

**検討する小論点:**

* 書き方の形:
  * **選択肢 1: `selected: bool` 属性**（推奨方向）— 既存の真偽値
    バインディングに乗り、新しい部品が不要。
  * 選択肢 2: 独立部品 `ToggleButton`。
  * 選択肢 3: 見た目テーマ経由のバインディング。
* 見た目の範囲: 選択中の見た目をどこまで作るか。M3 は**最小限**（色や枠の
  違いで判別できる程度）に留め、本格的なテーマは M5。**最小の見た目合格線
  （背景色だけ / 枠線だけ / 色＋枠）は構文だけでなく owner に見せて確定**する
  （[spec.md](../../requirements/spec.md) の Out-of-scope §Visual が「装飾なし
  最小表現、具体形は phase pre-doc で詰める」と定める）。
* 適用対象: タブ風セクションが中心だが、
  [gallery-wireframe.html](../../requirements/gallery-wireframe.html) は
  **サムネイルの強調表示**も選択中状態の surface として持つ。サムネイル選択も
  A10 の再現対象に含めるか、M3 ではタブに限定しサムネイルは最小近似にするかを
  **明示**する。
* 排他（タブ風グループ）「1 つだけ選択中」の表し方 — **スパイクで裏取りして
  から選択肢を確定する**（次の bullet）。**排他の*振る舞い*を gallery のタブ帯で
  見せることは M3 対象**だが、**排他を*DSL のグループ概念*（RadioGroup / TabBar
  等）として持つことは M3 out**（機能幅、defer）。出荷済みサーフェスの
  事実 — 式に比較・等値演算子 `==` が無い（`HandlerExpr` は算術複合代入のみ。
  [wasamo-ir/src/lib.rs](../../../../wasamo-ir/src/lib.rs)）、handler は状態への
  ブロック代入のみ（`clicked => { root.x = …; }`。[counter.ui](../../../../examples/counter/counter.ui)）、
  表示中に host が component state を書く public API が無い
  （[host-state-boundary.md](../../../../docs/notes/host-state-boundary.md)）— を
  踏まえると、M3 で実際に採れる形は:
  * **案α: 手書きブロック代入**。タブごとに独立 bool 状態を持ち、各 `clicked` が
    自分を true・他を false にする（3 タブで 3×3 代入。新サーフェスゼロだが
    O(N²)・手書きで脆い）。
  * **案β: A10 の実証手段を単一トグルにする**。selected の binding 駆動は単一
    Button のトグル（on/off）で陽性対照を証明し、タブ帯は静的強調にして排他は
    見せない（最小化）。
  * **案γ: 排他を楽にする最小サーフェス**（`==` 等値式 / 単一選択グループ）
    は **M3 out・trigger 付きで defer**。
  現状の「ホスト側ロジック vs DSL グループ概念」の 2 択は、**ホスト state 書き込み
  経路が M3 に無いため廃する**（ホスト経由にすると selected が binding 駆動でなく
  なり A10 の趣旨も崩れる）。最終選択はスパイク結果を見て DD-001 で確定。
* **検証スパイク（DD-001 の比較・採択より前に必須。実装計画の先頭ではなく
  pre-DD）**。スパイクが DD-001 の選択肢集合そのものを決める材料なので、実現不能な
  案を先に採らないよう **DD-001 の比較の前**に置く。`selected` 未実装の段階で
  selected の見た目までは検証できないので、**二段構え**にする:
  * **段階 1（pre-DD・`selected` 実装前）**: 出荷済みサーフェスだけで、**3 タブ
    相当の排他*状態*** を表現できるかを確認する — タブごとに独立 bool state を持ち、
    各 `clicked` のブロック代入で「1 つだけ true・他は false」になるか。`selected`
    属性はまだ無いので、状態の観測は **conditional `if` を推奨**（タブ状態に応じて
    マーカー subtree を出し入れする）。`enabled` を観測に使う場合は、**クリック
    対象のタブ Button 自身ではなく、別の観測用 Button / 表示部品 に限る**——
    disabled Button は `clicked` が発火しないため、未選択タブに `enabled` を縛ると
    クリック不能になり スパイクが不自然に失敗する
    （[widget.rs](../../../../wasamo-runtime/src/widget.rs)）。ここで案α/β/γ の
    実在性を確定し、DD-001 の選択肢集合をこの事実に合わせて立て直す。**selected の
    見た目はこの段階では検証しない。**
  * **段階 2（A10 実装後）**: `selected: bool` を足したうえで、選んだ実証手段で
    selected の見た目が binding 駆動で連動する陽性対照（2 コマ）を撮る（§検証方針）。
  スパイク規律（着手前に実装の着地先を実読して選択肢の実在性を確認する）の適用。
* 診断（エラー検査）: `selected` が許されるウィジェット（ボタン）と、対応
  しないウィジェットに付けたときの拒否。
* 検証: 真偽値バインディング → `selected` 属性 → 見た目の変化を、純粋ロジック
  テスト / ヘッドレステスト / 陽性対照（選択を切り替えると見た目が連動する
  2 コマ以上）で示す。
* **Out of scope / 将来互換（明示して将来像を狭めない）**: `selected: bool` は
  M3 の**最小 surface** であって「今後唯一の選択部品モデル」ではない。DD-001 は
  次の余地を明示的に残す — (a) `Button selected` は最小属性として残しつつ将来
  `ToggleButton` / `SegmentedControl` / `TabBar` 等の意味部品を追加できる、
  (b) 選択時の見た目は M3 の暫定で M5 テーマ surface に吸収・上書きできる、
  (c) 排他制御は M3 では出荷済みサーフェスの範囲（手書きブロック代入、または
 実証手段の最小化）で表し、将来のグループ意味論（RadioGroup / TabBar 等）を
  設計できる、
  (d) accessibility / focus / input semantics は M4 以降で再設計できる。

### DD-M3-P8-002 — DSL 仕様書の公開ドラフト昇格：予約と未確定部分の見せ方

**問い:**
`docs/dsl_spec.md` を公開ドラフトに昇格するとき、(a) M4 向け構文を予約するか、
(b) 未確定 / 暫定の将来機能をどう位置づけて書くか、(c) 公開版の状態マーカーと
社外読者チェックの合格線をどう定めるか。

**Phase 8 の問いである理由:**
A12 は編集作業ですが、最初の公開ドラフトは社外の読者や下流ツール（M5 の VS Code
拡張）が依拠し始める**最初の公開契約**です。「何を確定として書くか」は後で変えると
破壊的変更になるため、未確定部分を正直に未確定と書く判断が要ります。計画は M4
構文の予約を「**予約するか、見送ったと記録するか、明示的に決めよ**」と求めて
います（黙っていると「予約しない」が既定）。

**検討する小論点:**

* **M4 構文の予約**: 公開ドラフトで M4 向けの構文を予約するか、見送ったと
  記録するか（計画の明示要求。沈黙は「予約しない」が既定）。方針として、
  **予約は最小限**に留め、未確定のものは「予約（強い約束）」ではなく
  **将来ノート（future note）**として書く（予約しすぎは M4 の設計空間を狭め、
  予約しなさすぎは互換期待を曖昧にする——その中間を最小予約で取る）。
* **PM-2 の Grid 2 形式**: `Cell` ＋ 直接 `slot.*` の 2 形式を「受理する集合」
  として述べつつ、ラッパー規則は **1.0 前に決める暫定** と明記する
  （[constraints.md §3](./constraints.md)）。
* **サイズ指定（width/height）**: Fill 既定のサイズを確定として書かず、明示
  サイズを **未確定の将来機能**として位置づける
  （[constraints.md §2](./constraints.md)、Problem B Vision DR と連動）。
* **既定の寄せ方の非対称**（Grid は `stretch` / ZStack は `center`）: 正確に
  記述し、社外読者にとって説明可能か / 説明負債かを**明示的に判断**する
  （[constraints.md §4](./constraints.md)）。
* **配置の綴り**（`h-align` 等）: 公開前最後の改訂機会として、維持するか
  改めるかを**明示判断**（[constraints.md §5](./constraints.md)、推奨 ＝ 維持）。
* **配置のバインド可否 / 互換の位置づけ**: 配置は定数であると正確に記述し、
  ドラフトを「永続確定の互換約束」と誤読させない
  （[constraints.md §7](./constraints.md)）。
* **状態マーカーと変更履歴**: `status: public-draft`（または同等）のヘッダ
  マーカーと M3 の変更履歴（CHANGELOG）追記。
* **社外読者チェックの合格線**: 仕様書だけで（C ABI を備えた仮想ホストに対して）
  M3 の機能を再現できるか。できなければ編集の残作業。

**明示サイズの Vision DR はフェーズの判断項目ではない。** Problem B（明示
サイズ）の**責務の割り当て**はロードマップに関わる全体方針なので、フェーズの
決定記録ではなくマイルストーン横断の **Vision DR** で行います（DD-V-022 と同じ
やり方）。Phase 8 の方向づけ段階でそれを起票しますが、判断項目（DD）には
含めません。DD-002 が担うのは「公開ドラフトでサイズ指定をどう書くか」という
編集面だけです。

---

## Phase 8 の対象範囲（scope）

### 含むもの（In scope）

* **選択中状態（A10）**: 選んだ書き方の実装一式（構文解析・検査・変換・実行時・
  例の同期）。仕様書だけでなく実装まで。
* **ギャラリー統合（A1）**: 各フェーズの画面を 1 つの
  `examples/gallery/gallery.ui` に統合し、**3 言語（C / Rust / Zig）**で通しで
  動かす。統合先は wireframe の **Photo Gallery ターゲットアプリへ畳み込む**
  ことであって、検証画面集 / dashboard を作ることではない。
* **各フェーズ用の検証画面の片付け**: P5 のフッター切り抜き / P6・7 の
  ライトボックス / P7 の動的リスト / **P7b の配置デモ** など使い捨ての検証画面と
  撮影スクリプトを一掃し、レイアウト依存の座標を取り直す
  （[constraints.md §6](./constraints.md)）。
* **仕様書の編集（A12）**: 仕様書を公開ドラフト品質へ磨き、未確定の機能を
  暫定 / 予約として正直に提示（DD-002）。状態マーカー ＋ 変更履歴。
* **明示サイズの Vision DR 起票**（マイルストーン横断。起票と論点固定のみ、
  最終確定は別ゲート）。
* **マイルストーン締めの準備**: 先送り項目を M3 の `handoff.md` へまとめる
  経路の維持（PM-2 のラッパー規則、Problem B の最終確定先など）。

### 含まないもの — きっかけ付きで M4 以降へ送る（Out of scope）

[constraints.md 末尾表](./constraints.md)の前送り維持項目が正本です。要点のみ
再掲（責務先・きっかけは constraints が正本）:

| 先送り項目 | 発火のきっかけ | 責務先 |
|---|---|---|
| 明示サイズ（width/height）**の実装** | 部品既定以外のサイズを要する具体アプリ、または M6 の ABI 凍結準備の開始 | Problem B Vision DR が割り当てるマイルストーン（Phase 8 は起票のみ） |
| PM-2 ラッパー規則の**確定**（PM-1/PM-3 へ寄せる） | 新しい入れ物がラッパー形式を欲する / 公開のコード構築 API / 1.0 到達（最終期限） | 1.0 前（M3 handoff 経由） |
| 既定寄せ方の統一の**実装** | 公開ドラフトが説明負債と判断したら残課題として送付 | 後続のレイアウト挙動フェーズ |
| 画像部品 / 素材パイプライン / スクロールバー部品 / 入力 / モーダル / テーマ | 各 M4/M5 の基準 | M4 / M5 |
| 公開のコード構築 API / ABI | 構築 API が試験段階を超えて昇格 | M6 の ABI 凍結準備 |
| 汎用モディファイア / ユーザー定義の入れ物 / レイアウト以外の親データ / キー付き同一性 / レイアウトアルゴリズム変更 | 各々の具体的な要求 | 後続の DSL / レイアウトフェーズ |

新しいレイアウト部品・新しい寸法計算は作りません。**lightbox（写真の拡大
オーバーレイ）は M3 の対象**です — ボタンクリックのハンドラで真偽値を切り替え、
lightbox の部分木（subtree）を出し入れする**構造の証明**までが M3
（[spec.md](../../requirements/spec.md) の Interaction 節が close / prev / next
を Button click ハンドラで表現すると定める）。M4 へ送るのは **実画像の当たり判定
（hit-testing）/ モーダルフォーカス / スワイプ等のジェスチャ磨き込み**です。
これら M4 機能は仮置き規律（Box ＋ Text ＋ 素のボタン）で構造の経路だけ証明し、
挙動は M4 のままにします。

---

## 検証方針

Phase 8 の検証は、「**M3 の機能がすべて 1 つのギャラリーで通しで動く**」ことと、
「**仕様書だけで社外読者が再現できる**」ことを示します。

### 目視による通し証明

* **選択中状態（A10。陽性対照が要）**: DD-001 が決める実証手段（タブ帯、または
  単一トグル）で選択を切り替え、選択中の見た目が**連動して変わる** 2 コマ以上を
  撮ります。1 コマだけでは見た目を固定で描いても同じに写るので、真偽値
  バインディングが属性を動かしている証明になりません（Phase 6 の表示切り替え /
  Phase 7 の反復と同じ型の陽性対照）。タブ帯を実証手段にする場合は、排他
  （クリックで他が外れ 1 つだけ選択中になる）も同じ 2 コマで見せます。
* **ギャラリー統合（A1）**: 統合したギャラリーを起動し、スクリーンショット ＋
  解析。「DSL → 中間表現 → 実行時」の経路（ホスト側で手組みしない）で全画面が
  出ることを確認。見た目は
  [gallery-wireframe.html](../../requirements/gallery-wireframe.html) と照合し、
  **余計な検証 UI / デモメニューが残っていないこと**も確認する。確認する
  ギャラリーの状態は **既定表示 / lightbox 表示 / 選択切り替え 2 コマ /
  折り返し・あふれ**（この状態セットは初期案で、**FD-8-G(1) の合意で確定し
  実装計画に反映**する）。**3 言語のうち UI レビューは代表 1 言語で行い、残り 2
  言語は同一表示（同じ見た目）/ 回帰なしの確認に留める**。
* **レイアウト骨格の技術 smoke（owner UI 確認の前）**: full gallery を本組みする
  前に、wireframe 骨格だけ（Grid フレーム / WrapPanel / ScrollView / ZStack /
  Box `aspect` を 1 画面に畳んだ最小構成）を代表 host で表示し、**0 サイズ化 /
  clip 破綻 / aspect 中断 / scroll 破綻**が無いかをアシスタントが先に潰す
  （Problem B 由来の Fill→0 collapse や aspect abort を早期発見する）。これは
  owner UI レビューではなく**技術 smoke** で、FD-8-G(2) の初回 owner 表示確認の
  前提とする。
* アシスタント自動の証拠は、起動 ＋ スクリーンショット（`CopyFromScreen`、
  モニタごとの DPI 対応）＋ 解析を含めます。オーナーの目視確認の代わりには
  なりません。規範の核は [AGENTS.md §Testing rules](../../../../AGENTS.md)、
  撮影機構と陽性対照の原理は
  [verification-environments.md Observation 4](../../../../docs/notes/verification-environments.md)。

### A1 機能対応表（どの画面でどの M3 機能が通るか）

余計な UI を足さずに A1 を検証するため、Photo Gallery の各部分と、そこを通る
M3 機能を対応づける。統合作業はこの表を埋めることであって、表にない検証画面を
足すことではない。

**この表は framing 時点の初期対応仮説 / 監査足場であり、最終 UI 仕様そのもの
ではない。** FD-8-G(1) の wireframe 逸脱合意、および DD-001 の選択中状態の
適用対象判断により、行の粒度・対応箇所・仮置き扱いは実装計画で更新され得る。
ただし「A1 の M3 機能を**余計な検証 UI なしに Photo Gallery ターゲットアプリ内で
通す**」という目的はここで固定する。

右端の列は、その部分が
[gallery-wireframe.html](../../requirements/gallery-wireframe.html) の**どこ**を
指すかを示す（**(1)(2)(3)** は wireframe 自身の番号付き callout。Grid 全体
フレーム・タブ・強調サムネイル・配置は wireframe に番号が無いので、領域名と
座標で補足する）。

| Photo Gallery の部分 | そこを通る M3 機能 | wireframe 上の位置（gallery-wireframe.html） |
|---|---|---|
| 全体フレーム | Grid（star sizing / 連結 span） | default 図の窓全体を **上＝タブ帯(高さ40) / 中＝サムネイル領域(伸縮) / 下＝ステータス帯「218 photos」(高さ20)** に分ける行割り（callout 番号なし＝roadmap A1 の "Grid (overall frame)"） |
| サムネイル一覧 | WrapPanel ＋ ScrollView（折り返し ＋ ビューポート） | default 図 **callout (1)**（5×4 のサムネイル格子）＋「WrapPanel proof strip」節の **narrow / overflow** 図（ScrollView の clip＋scroll） |
| サムネイル生成 | 繰り返し（iteration）＋ コレクションバインディング | 同じ格子の各セル（節「Grammar &amp; binding surfaces」の **Repetition** ＝ thumbnail collection を反復生成） |
| ライトボックス | ZStack ＋ 条件レンダリング（真偽値で出し入れ） | **lightbox 図** の **callout (3)**（scrim ＋ 中央写真 ＋ キャプション ＋ `< > x` を重ねる）。条件レンダリング ＝ lightbox subtree が `bool` で出入り |
| タブ風セクション（サムネイル強調を含めるかは DD-001 で確定） | 真偽値 ＋ 選択中状態（`selected`） | default 図 上部の **「All(選択中) / Albums / Favorites」タブ帯**、および格子内の **強調された 1 枚（2 行目 3 列目の青いセル）**（節「Grammar &amp; binding surfaces」の **Button selected state**） |
| 写真らしき領域 | Box（`aspect`）＋ Text の仮置き | default 図 **callout (2)**（正方サムネ Box `aspect:1/1`）＋ lightbox 図 中央の **`[photo]` プレースホルダ（Box `aspect 4:3` ＋ Text）** |
| 子の配置 | `slot.*`（Grid `Cell` / ZStack） | Grid 全体フレームの行/列割り当て（`Cell`）と、lightbox 中央写真の ZStack 中央寄せ（`slot.h-align` / `slot.v-align`） |

なお、タブ帯内部の横並びは **HStack**、lightbox キャプションの縦並びは
**VStack**（wireframe「Supporting layout primitives」）で、いずれも M2 baseline。
本表で新しいのは選択中状態（`selected`）と配置（`slot.*`）であり、線形 stack
自体は M3 surface ではない。

### オーナー確認ポイント（packet G の工程化）

phase 末だけに寄せず、実装計画に次の確認を**工程として**置く（packet G の 5
項目に対応）。

1. **wireframe 逸脱の合意**（初回表示の前提）: M3 には実画像・不透明度・
   スクロールバー部品・モーダルフォーカス・本格テーマが無いので、wireframe の
   「忠実に再現する箇所」と「M3 では仮置きとして許容する箇所」を**初回表示の前**に
   owner と合意する（後で揉めない足場）。**この合意結果を A1 機能対応表と
   Phase 8 実装計画へ反映し、以後の UI 確認は更新後の対応表を基準に行う。**
2. **初回表示**: 統合ギャラリーが代表 1 言語で初めて表示できた時点で owner の
   UI 確認（方向ずれを早期に発見する。phase 末では遅い）。
3. **陽性対照**: 選択中状態 / lightbox の切り替え前後 2 コマを owner 確認。
4. **公開ドラフト ＋ M3 handoff**: DD-002 の社外読者チェックに加え、M4 構文の
   予約・未確定表現の強さ・PM-2 / Problem B / 既定寄せ方の先送り説明は owner の
   判断余地が大きいので、公開ドラフトと M3 handoff のドラフトを phase close 前に
   owner 確認。
5. **最終スモーク**: 上記のギャラリー状態セット（既定 / lightbox / 選択 2 コマ /
   折り返し・あふれ）で最終の人間目視スモーク。

### 純粋ロジック / ヘッドレステスト

* 真偽値バインディング → `selected` 属性 → 見た目の伝播（既存の文字列 / 真偽値
  バインディングの証拠パターンの拡張）。
* `selected` を対応しないウィジェットに付けたときの拒否（実際に失敗する
  テスト）。
* 既存機能に回帰がないこと（ギャラリー統合で各画面の寸法計算 /
  バインディングが壊れていない）。
* **`selected` 伝播監査（A10 の中心証拠）**: `Button.selected` は parser /
  check / lower / IR emit / runtime loader / widget visual / ホスト間の見た目一致 を
  跨ぐ横断変更。implementation-gates の call-site audit table（trap #1）に加え、
  次の 3 点を firing test / 陽性対照で固定する — (i) **Button 以外に `selected`
  を付けたら reject**、(ii) **bool binding の変化が visual に届く**、(iii)
  **C / Rust / Zig で同じ見た目（ホスト間の一致）**。

### 仕様書の編集ゲート（A12 の検証）

* **社外読者チェック**: 仕様書だけで M3 の機能を再現できるか。できなければ
  編集の残作業（マイルストーン完了基準 5）。
* 各フェーズの仕様同期が監査可能（各決定記録が「更新した仕様セクション」への
  参照を持つ）。
* 未確定機能（PM-2 / サイズ指定 / 既定寄せ方 / 綴り）が暫定 / 予約として
  **正直に**書かれているか（DD-002 の判断結果が反映済みか）。

---

## リスク

### R1. 編集が「あとで書く」に流れる
A12 は編集ゆえコードより後回しにしやすい。フェーズ完了基準の「仕様同期」は
厳格ゲート（[plan.md §Risks](../../plan.md)）。仕様文が "TODO" のフェーズは
閉じません。

### R2. 未確定部分を「確定」と書いてしまう
最初の公開ドラフトで PM-2 / サイズ指定 / 既定寄せ方 / 綴りを確定として書くと、
後で変えたとき公開済みの書き方の破壊的変更になります。DD-002 が「暫定 / 予約と
正直に書く」を明示判断として閉じます（[constraints.md §1〜§7](./constraints.md)）。

### R3. 選択中状態の作り込み過剰
見た目やタブグループを作り込むと M5 のテーマ / M4 の入力に食い込みます。M3 の
見た目は最小限（色 / 枠）に留め、専用グループ部品は作りません（C）。排他の
実証手段（タブ帯か単一トグルか）と表現法は **事前スパイクと DD-001 で確定**し、
ここで先に独立 bool 等の実装形を決め打ちしません。

### R4. 明示サイズの Vision DR が M3 の完了を止める
明示サイズの責務割り当てはマイルストーン横断の全体方針で、確定を急ぐと M3 の
締めが遅れます。Phase 8 は**起票と論点固定まで**を責務とし、最終確定は別ゲート
（packet D）。公開ドラフト側は「未確定の将来機能」と書くだけで先に進めます。

### R5. ギャラリーが大きすぎて CI で回らない
M3 のギャラリーは Hello Counter より大きい。通しの検証は単一の固定ケースとし、
各フェーズのヘッドレステストは各画面を対象にします（[plan.md §Risks](../../plan.md)）。

### R6. `selected` 追加が多層をまたぐ横断変更
`Button.selected` は parser → check → lower → IR emit → runtime loader →
widget visual → ホスト間の見た目一致 を跨ぐ。implementation-gates の call-site audit に
加え、上記の **selected 伝播監査**（非 Button reject / bool 変化が visual に届く /
3 ホストで同じ見た目）を A10 の中心証拠として framing 段階からリスク対象に置く。

### R7. full gallery で潜在 collapse が表に出る
Grid / ZStack（Fill 既定）を 1 画面に畳むと、Problem B の Fill→0 collapse や
aspect abort が初めて顕在化しうる。owner UI 確認の前に**レイアウト骨格の技術
smoke**（§検証方針）で 0 サイズ化 / clip / aspect / scroll 破綻を洗い出す。
ただし M3 は新しい寸法計算を足さない方針なので、**既存サーフェスで直せるものは
直し、直せないものは A1 対応表の仮置き / owner 仮置き合意（FD-8-G(1)）/
Problem B Vision DR へ切り分ける**（Phase 8 で layout engine を改修しない）。

### R8. 排他タブを実証手段にすると出荷済みサーフェスだけでは重い / 不自然
`==` も host state 書き込みも無く、排他は手書きブロック代入（O(N²)）でしか
表せない可能性がある。3 タブで成立しても作者が書く形として不自然なら、
gallery のタブ帯を排他の実証手段にするのは過大になりうる。**緩和策 =
事前の実現性スパイク**（着手前に実在性とコストを測る）、**退避策 =
A10 の実証手段を単一トグルにする（案β）か、楽にする糖衣を M3 out で defer
（案γ）**。この実現性は R6（`selected` 横断変更）とは別の、実証手段
自体の成立性リスク。

---

## 取り込んだ文書一覧

* [constraints.md](./constraints.md) — §2.1 の制約引き継ぎ。本フレーミングの
  直接入力（引き込み項目 §2〜§7 / 前送り維持表 / プロセス学び）。
* [phase-7b handoff.md](../../phase-7b/implementation/handoff.md) — 後続へ送る
  候補、Phase 8 向けメモ（PM-2 を暫定と明示せよ / 明示サイズの Vision DR を
  Phase 8 が表に出せ）。
* [phase-7b framing.md](../../phase-7b/requirements/framing.md) — 先送り項目の
  正本（対象外の表）と packet 構造の前例。
* [plan.md](../../plan.md) — Phase 8 の行（3 つの成果 / M4 予約を明示判断せよ）、
  受入れ基準とフェーズの対応、マイルストーン完了基準、リスク。
* [_roadmap.md](../../../_roadmap.md#m3-dsl-surface) — A1 / A10 / A12 / A13 の
  正本。
* [spec.md](../../requirements/spec.md) — Photo Gallery ターゲットアプリ仕様。
  「最終フェーズで full app として閉じる」「lightbox の close/prev/next は
  Button click ハンドラ」「選択中状態の見た目は装飾なし最小」などの境界の正本。
* [gallery-wireframe.html](../../requirements/gallery-wireframe.html) — 採択済み
  Photo Gallery の視覚仕様（既定 / lightbox / 折り返し・あふれ / サムネイル強調
  の各状態）。A1 / A10 の視覚照合の正本。
* [author-controllable-sizing.md](../../../../docs/notes/author-controllable-sizing.md)
  — Problem B の証拠と Vision DR 起票タイミング（§7.2）。
* [architectural-family.md](../../../../docs/notes/architectural-family.md)
  — きっかけ 1（M3 の仕様策定）。Phase 8 の公開ドラフトで再確認。
* [docs/dsl_spec.md](../../../../docs/dsl_spec.md) §4.16 /
  [docs/architecture.md](../../../../docs/architecture.md) §6.8.6 / §6.8.4
  — 7b が同期した配置の書き方。Phase 8 は読むだけ。

---

## フレーミングの次にやること

オーナーがこのフレーミングに合意したら、次の順序で進めます。**DD-001 の比較・
採択より前に 事前の実現性スパイクを実施する**のが肝で、スパイクを飛ばして
DD-001 を書き始めない。

1. **DD-001 用 事前の実現性スパイク（段階 1）を実施**する。出荷済み
   サーフェスだけで 3 タブ相当の排他*状態*が表現できるかを確認（`selected`
   実装前ゆえ状態観測は **conditional `if` 推奨**。`enabled` は観測用 表示部品
   に限る——クリック対象に縛ると disabled で `clicked` が不発火。§論点一覧 DD-001）。
2. **スパイク結果を記録**し、案α/β/γ と A10 の実証手段（タブ帯か単一トグルか）の
   **選択肢集合を確定**する。実現不能な案はここで落とす。
3. その後に **DD-001 を `Status: Proposed` でドラフト**する
   (`decisions/dd-m3-p8-001-button-selected-state-surface.md`)。owner review を
   経て `Accepted` に進め、設計同期（Moment 1）。
4. **DD-002 / preamble / Problem B Vision DR は並行可**（DD-001 の比較内容を
   固定しない範囲で）:
   * `decisions/preamble.md`
   * `decisions/dd-m3-p8-002-dsl-spec-public-draft-promotion.md`
   * 明示サイズ（Problem B）の **Vision DR** を
     `process/cross-milestone/decisions/` に起票（フェーズ決定記録とは別の
     レビュー単位。M3 完了をブロックしないマイルストーン横断の全体方針）。

決定記録のドラフトはいずれも `Status: Proposed` から始め、owner review →
`Accepted` → 設計同期（Moment 1）の順。
