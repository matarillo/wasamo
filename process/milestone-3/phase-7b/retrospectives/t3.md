---
phase: M3-Phase 7b
task: T3
title: Runtime SlotData storage migration
date: 2026-06-22
scope: task-end
merge_target: feat/m3-phase-7b
---

# T3 レトロスペクティブ

タスクブランチ: `feat/m3-phase-7b-t3`

T3 は、T2 で canonical IR 側へ移した child-slot placement を runtime/layout
の構造にも反映し、placement を親側の並列 vector や子 node field ではなく
child slot record に保持する migration として実施した。実装前に
`implementation/plan.md` の T3 責務を再整理し、start gate / end gate は
`implementation/log.md` の T3 entry に auditable artifact として記録した。

コミット:

- `eb924b8 Migrate runtime slot storage`

## チェックリスト（task-end、項目 1〜11）

1. **今回の主な学び。** slot 情報を child slot record に寄せる方針自体は正しかったが、runtime と pure layout test の両方で同じ slot 構造を扱うには `ChildSlots` wrapper が必要だった。compile-forced drift check は、旧 storage の取り残しと test helper の前提差分をかなり早く露出させた。
2. **仕様文書の変更。** なし。`docs/architecture.md` / `docs/abi_spec.md` / `docs/dsl_spec.md` は未変更。T3 では process の plan/log のみ更新した。
3. **post-commit 検証。** green。post-commit state に対して `cargo fmt --all -- --check` green、`cargo clean` 実施、`cargo build --release --workspace` green、`cargo build --workspace` green、`cargo test --workspace` green。build 中に `wasamo` linkable target warning と `wasamo-sys` import lib warning が出たが、既存の build-order warning として成功終了した。実装 close 前には `git diff --check` も CRLF warning のみで green。
4. **PO に相談すべき設計判断・トレードオフ。** なし。T3 の再切り分けは、T2 carry-over と Phase 7b の実装責務に沿った runtime/layout 側 migration で閉じた。
5. **タスク外のリファクタ・構造変更。** なし。`WidgetNode` / `LayoutNode` の storage migration と、それに直接必要な loader/test 更新に限定した。レビュー follow-up で明示した Grid build-path 統合は storage migration に必要な in-task 構造変更であり、タスク外リファクタではない。
6. **追加 DD の要否。** なし。既存の child-slot payload 方針の実装化であり、新しい設計判断は ADR 追加を要しない。
7. **新規 Proposed ADR 項目 / 昇格。** なし。
8. **マイルストーン AC / フェーズ構成の追加・変更。** なし。
9. **後続へ持ち越す仮実装・近似・新規 `dead_code`。** なし。T3 から owner 不明の未解決点は残していない。Grid structural mutation と GUI/owner evidence は既存の T4/T5/T7 owner に前送り済みであり、T3 の仮実装ではない。
10. **新規の cross-task / cross-phase 設計制約。** あり。
    - **制約**: Runtime/layout placement は child slot が所有し、今後 placement-bearing child mutation は child node field や parent parallel vector を再導入せず `SlotData` を slot に保持する。
    - **エビデンス**: `WidgetNode::children` / `LayoutNode::children` migration、旧 storage grep (`cell_placements|zstack_placement|insert_child_with_zstack_placement|zstack_placement_for_parent`) の不在、`insert_stores_*_slot_data_on_the_slot` と ZStack replacement integration test。
    - **配置先**: フェーズ終了時反映 (`phase-sync`)。T7 の docs sync / phase-end sync で implementation-synced な architecture 記述に反映する候補。
    - **制約**: Future Grid structural mutation path は T3 の child-slot storage を前提に side-effect / drift gates を再実行し、parallel placement vector を復活させない。
    - **エビデンス**: T3 end gate の carry-forward ownership で Grid mutation path を T7 owner に設定し、T3 source scan では runtime storage migration だけを閉じた。
    - **配置先**: 前送り候補 (`carry-forward`)。phase-end retro item 15 で handoff 対象に含めるか確定する。
11. **後続タスクリストの見直し。** 不要。T4/T5/T7 の owner は plan/log に残っており、T3 の未解決点に owner 不明はない。

## 目標・前提・計画仮説の再点検

> 後続タスクの retrospective でも流用するダブルループ振り返り欄。
> 実行の巧拙（シングルループ）でなく、**設定した目標・前提・事前計画
> （= 検証すべき仮説）自体が妥当だったか**を、反証可能な signal とともに
> オーナーと問い直す。下の 5 サブセクションがひな型。

### 観測された事実（反証可能な signal）

当初 plan の T3 は「Seam A の残り」寄りに見えたが、T2 close 時点の
carry-over と source shape を照合すると、T3 の責務は IR parser/author
surface ではなく runtime/layout storage migration だった。旧 storage grep
と compile error の両方が、placement の残存箇所を機械的に示した。

### 自己分析（どの層が外れたか）

外れたのは実装手順ではなく、plan に残っていた task boundary の解像度だった。
T2 で canonical IR が閉じた後も、plan の文言は「まだ IR migration が主責務」
のように読めたため、開始時に carry-over を基準に T3 を切り直す必要があった。

### 計画仮説・前提の問い直し（オーナーと）

今回の仮説修正は妥当だった。今後も plan の checkbox は仮説として扱い、
前タスク log の carry-over と current source grep を task boundary の上位
signal として使うべき。特に multi-task migration では、前タスクで閉じた
layer と次に残った layer を毎回分け直す。

### 反証可能な是正テスト

次の migration task でも、開始前に「旧 storage / old helper / stale wording」
の grep と、log carry-over owner table の照合を行う。plan wording と機械出力
が食い違う場合は、実装前に plan/log へ task recut を記録する。

### 後続タスク / オーナーへ共有すること

T4/T5/T7 は、T3 後の runtime/layout storage を前提にする。Placement を扱う
後続実装で `SlotData` 以外の parallel storage を増やす必要が出た場合は、まず
task boundary または ADR 側の仮説が外れていないかを確認する。

## マージゲート

Review lane は **full independent review**。T3 は runtime structural migration
であり、`WidgetNode` / `LayoutNode` の子 storage と layout semantics に触れたため、
merge 前に別エージェントの full independent review が必要。task checklist は完了
したが、オーナーの明示承認なしでは `feat/m3-phase-7b` へ no-ff merge しない。
